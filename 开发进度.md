# 📋 钱程似锦（Qiancheng）开发进度记录（曾用名 FundPilot AI，2026-09-23 起改名）

> 本文件记录每个开发阶段（Phase）完成后的反馈总结，持续更新。
> 项目按 README.md 定义的 Phase 1 → Phase 8 逐阶段开发。

---

## ✅ Phase 1：项目基础（已完成）

- **完成日期**：2026-09-22
- **状态**：已完成，全部测试通过，可正常运行
- **目标**：搭建项目骨架，跑通「前端 → API → 前端渲染」完整链路

---

### 一、交付文件清单

#### 根目录配置

| 文件 | 作用 |
|---|---|
| `requirements.txt` | 依赖清单：fastapi、uvicorn、pydantic、httpx、openai、python-dotenv、pytest |
| `.gitignore` | 屏蔽 `.env`、`.venv/`、数据库文件等，防止密钥泄漏到 GitHub |
| `.env.example` | 环境变量模板（AI_API_KEY / AI_BASE_URL / AI_MODEL），为 Phase 6 预留 |
| `run.py` | 一键启动脚本：`python run.py` 启动服务并自动打开浏览器 |
| `start.bat` | Windows 双击启动脚本（资源管理器中使用） |

#### 后端（分层结构，为后续扩展留空间）

| 文件 | 作用 |
|---|---|
| `app/main.py` | 主程序：创建 FastAPI、注册路由、加载 .env、托管前端页面 |
| `app/api/fund.py` | 基金路由 `GET /api/funds` |
| `app/api/portfolio.py` | 持仓路由 `GET /api/portfolio/summary` |
| `app/api/ai.py` | AI 路由 `GET /api/ai/status`（只检查配置，不返回密钥） |
| `app/api/health`（在 main.py 中） | 健康检查 `GET /api/health` |
| `app/services/fund_service.py` | 基金数据服务（Phase 2 在此接入真实数据 API） |
| `app/services/portfolio_service.py` | 持仓业务逻辑（Phase 3/4 接数据库和收益计算） |
| `app/services/ai_service.py` | AI 服务（Phase 6 接 GPT） |
| `app/models/fund.py` | 基金 Pydantic 模型（FundBrief） |
| `app/models/portfolio.py` | 账户概览 Pydantic 模型（PortfolioSummary） |
| `app/database/database.py` | 占位模块（Phase 3 实现 SQLite 存储） |
| `app/utils/calculator.py` | 占位模块（Phase 4 实现收益计算公式） |

#### 前端

| 文件 | 作用 |
|---|---|
| `frontend/index.html` | Dashboard 骨架：总资产/今日收益/累计收益卡片 + 基金列表 + 免责声明 |
| `frontend/js/app.js` | 请求后端 API 并渲染数据（含空状态、加载失败提示，涨红跌绿） |
| `frontend/css/style.css` | 页面样式，手机端自适应 |

#### 测试与其他

| 文件 | 作用 |
|---|---|
| `tests/test_main.py` | 4 个接口测试（健康检查、账户概览、基金列表、首页） |
| `data/.gitkeep` | 保证 data/ 目录能提交到 Git |

---

### 二、验证结果（已完成）

- ✅ 自动测试：`pytest tests/` → **4 passed**
- ✅ 首页 `http://127.0.0.1:8000` → 200
- ✅ Swagger 文档 `http://127.0.0.1:8000/docs` → 200
- ✅ 4 个 API 均返回正确 JSON：
  - `GET /api/health` → `{"status": "ok", "version": "0.1.0"}`
  - `GET /api/portfolio/summary` → 空账户数据（全 0）
  - `GET /api/funds` → `[]`（暂无持仓，正常）
  - `GET /api/ai/status` → `{"configured": false, ...}`（未配置 AI，正常）
- ✅ CSS / JS 静态文件正常加载
- ✅ `run.py` 启动 + 自动打开浏览器验证通过

---

### 三、如何运行（三种方式任选）

```powershell
# 方式 1：一键脚本（推荐，自动打开浏览器）
python run.py

# 方式 2：双击 start.bat（资源管理器中）

# 方式 3：标准方式
.venv\Scripts\activate
uvicorn app.main:app --reload
```

访问地址：
- 首页：http://127.0.0.1:8000
- 接口文档：http://127.0.0.1:8000/docs

### 四、如何测试

```powershell
& .venv\Scripts\python.exe -m pytest tests/ -v
```

---

### 五、开发过程中解决的环境问题（重要记录）

1. **嵌入式 Python 问题**：本机 PATH 中的默认 `python` 是嵌入式精简版
   （`D:\py\python-3.13.12-embed-amd64`），不含 venv 模块。
   **解决**：已将 `D:\conda`（完整版 Python 3.14）在 PATH 中上移至嵌入式版之前，
   现在 `python` 命令默认使用完整版。
2. **虚拟环境**：`.venv` 由 `D:\conda\python.exe -m venv .venv` 创建（Python 3.14.6），
   依赖已全部安装。
3. **README.md 曾被意外清空**：开发中发现磁盘上的 README.md 变成 0 字节（疑似编辑器误保存），
   已从对话记录中完整恢复（15KB），并同步补充了启动方式说明。

---

### 六、当前项目状态

- 前端 → API → Service → 前端渲染的完整链路已跑通（显示空账户，无假数据）
- 分层设计就位：后续基金 API 放 service 层、GPT 放 ai_service，不会污染 main.py
- 密钥安全：全部走环境变量，.env 已被 .gitignore 屏蔽
- README.md 与实际目录结构一致

### 七、下一阶段：Phase 2 基金数据

计划实现：

1. 调研可用的免费基金数据接口（优先天天基金等公开接口）
2. 在 `fund_service.py` 中用 httpx 实现：
   - 基金代码查询 / 基金基本信息
   - 最新净值、历史净值
   - 单日涨跌幅
3. 配套 Pydantic 数据模型与测试用例
4. 前端增加基金搜索/查询入口

---

## ✅ Phase 2：基金数据模块（已完成）

- **完成日期**：2026-09-22
- **状态**：已完成，23 个 pytest 全部通过，真实数据源实测通过
- **目标**：建立独立可替换的数据源层，获取真实基金公开数据

---

### 一、实际测试的数据源与结论（写代码前实测）

| 数据源 | 接口 | 实测结果 | 采纳情况 |
|---|---|---|---|
| 历史净值 | `api.fund.eastmoney.com/f10/lsjz` | ✓ 可用（需 Referer 头；数据在 `Data.LSJZList`，接口结构有变化） | ✅ 采用 |
| 实时估值 | `fundgz.1234567.com.cn/js/{code}.js` | ✗ **已失效**：HTTP 200 但返回"页面未找到" HTML | ❌ 不采用 |
| 基金搜索 | `fundsuggest.eastmoney.com/FundSearch/api/FundSearchAPI.ashx` | ✓ 可用（`Datas`，CATEGORY=700 为基金，`FundBaseInfo` 含类型） | ✅ 采用 |

**按预定降级规则处理**：估值接口失效 → Phase 2 实现「搜索 + 历史净值 + 最新已确认净值」，
`/valuation` 接口返回 `valuation_available: false`，全程无假数据。

### 二、数据源风险说明（重要）

当前使用的接口属于**公开网页接口，不是官方稳定开放 API**，无 SLA 保证：

- 请求需要带浏览器 User-Agent / Referer，否则可能被拒绝（403）
- 返回结构可能随时变化（本次历史净值接口就从 `Datas` 变成了 `Data.LSJZList`）
- fundgz 估值接口就是失效案例：能访问但跳转 notfound 页面
- 因此数据源层做了抽象（`FundDataSource` 基类），未来可替换为官方开放 API，
  且所有解析失败都会抛出明确异常而不是静默出错

### 三、新增/修改文件

| 文件 | 说明 |
|---|---|
| `app/services/fund_data_source.py` | **新增** 数据源层：`FundDataSource` 抽象基类 + `EastMoneyFundSource` 实现（历史净值/搜索，httpx + 10s 超时 + 异常分类 + 日志） |
| `app/utils/fund_parser.py` | **新增** 解析工具：`parse_jsonp_response`（JSONP→dict）、`to_float`（空值安全转换），解析失败抛 `FundParseError` |
| `app/services/fund_service.py` | **重写** 业务层：持有数据源实例（换源只改一行）+ 简单内存缓存（历史/搜索 5 分钟 TTL）+ 综合查询组合 |
| `app/models/fund.py` | **扩展** 新模型：`FundBasicInfo`、`FundNavItem`、`FundHistoryPage`、`FundValuation`、`ValuationResponse`、`FundDetail` |
| `app/api/fund.py` | **扩展** 4 个新路由（见下），统一错误码映射（404/400/503） |
| `app/main.py` | 添加 logging 配置 |
| `frontend/index.html` / `js/app.js` / `css/style.css` | 新增「基金数据查询」面板：代码查询详情 + 历史净值表格 + 关键词搜索（点结果自动填码） |
| `tests/test_fund_api.py` | **新增** 19 个 mock 测试（httpx.MockTransport，不发真实请求） |
| `tests/manual_test_fund.py` | **新增** 手动连通性验证脚本（真实请求） |
| `README.md` | 更新数据源现状说明 + Phase 2 标注完成 |

### 四、新增 API（均已实测真实数据通过）

| 接口 | 说明 | 实测结果 |
|---|---|---|
| `GET /api/funds/{code}` | 综合信息：名称+类型+最新净值+估值状态 | 000001 → 华夏成长混合，最新净值 1.3320（2026-09-21） |
| `GET /api/funds/{code}/history?page=1&page_size=20` | 历史净值分页 | total=6011，数据正确 |
| `GET /api/funds/search?keyword=半导体` | 基金搜索（已过滤股票） | 返回 10 条基金 |
| `GET /api/funds/{code}/valuation` | 实时估值 | 明确返回 `valuation_available: false` |

错误处理分类（实测验证）：基金不存在 → **404**；代码格式错误 → **400**；
数据源网络异常/接口失效 → **503**；不会出现笼统的 500。

### 五、测试结果

```
pytest tests/  →  23 passed（Phase 1 的 4 个 + Phase 2 的 19 个，全部 mock 不发真实请求）
```

覆盖：JSONP 正常/空响应/HTML/非法格式解析、净值解析（含空字段）、
无效代码、HTTP 403、网络异常、搜索过滤非基金、7 个 API 行为测试。

真实数据源连通性验证：`& .venv\Scripts\python.exe tests/manual_test_fund.py`

### 六、如何运行

```powershell
python run.py    # 或双击 start.bat
```

前端首页新增「基金数据查询」区域：输入 6 位代码查询详情和净值，支持关键词搜索。

### 七、遗留风险与下一步

- 估值功能暂缺：等找到可靠的公开估值数据源后在 `fund_data_source.py` 实现
- 数据源为公开网页接口，可能失效；抽象层已备好换源能力
- 缓存为进程内字典，重启即失效（个人项目够用，不引入 Redis）

### 八、下一阶段：Phase 3 我的持仓（未开始）

计划：SQLite 存储、基金增删改查、持有份额/成本、与真实净值联动计算市值。

---

## ✅ Phase 3：我的持仓 + 收益计算（已完成）

- **完成日期**：2026-09-22
- **状态**：已完成，53 个 pytest 全部通过，真实数据端到端验收通过
- **目标**：真实基金数据 + 真实个人持仓 + 正确收益计算

---

### 一、数据库结构

- 数据库：SQLite（`data/fundpilot.db`，SQLAlchemy 2.x，迁移 MySQL 只改 DATABASE_URL）
- 一张表 `portfolio_holdings`：

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | 自增主键 |
| fund_code | VARCHAR(10) UNIQUE | 基金代码（同基金只允许一条持仓） |
| fund_name | VARCHAR(100) | 基金名称（添加时从数据源自动获取） |
| shares | NUMERIC(18,4) | 持有份额 |
| cost_price | NUMERIC(18,4) | 持仓成本净值 |
| created_at / updated_at | DATETIME | 创建 / 更新时间 |

设计原则：**数据库只保存用户输入的持仓信息**，净值、市值、收益等会变化的数据全部实时计算，不落库。

### 二、收益计算（app/utils/calculator.py，全程 Decimal）

```
投入金额       = 份额 × 成本                    （2 位小数）
当前市值       = 份额 × 最新已确认净值           （2 位小数）
累计收益       = 市值 - 投入                     （2 位小数）
收益率         = 收益 ÷ 投入 × 100（投入 0 时返回 0，防除零）
净值变化       = 最新净值 - 上一交易日净值        （4 位小数）
今日收益估算   = 份额 × 净值变化                 （2 位小数）
```

- 所有资金运算使用 Decimal（ROUND_HALF_UP），**不使用 float**
- 今日收益不叫 realtime：估值数据源已失效，字段名统一为 `latest_nav_change`（基于最新两个交易日已确认净值的估算）

### 三、新增/修改文件

| 文件 | 说明 |
|---|---|
| `app/database/database.py` | SQLite 引擎 / 会话 / get_db 依赖 / init_db 建表 |
| `app/database/models.py` | PortfolioHolding ORM 模型 |
| `app/utils/calculator.py` | 全部 Decimal 收益计算函数 |
| `app/models/portfolio.py` | HoldingCreate / HoldingUpdate / HoldingResponse / PortfolioSummary |
| `app/services/portfolio_service.py` | 持仓 CRUD + 收益组装 + 账户汇总（净值实时调 Phase 2 数据源） |
| `app/api/portfolio.py` | 持仓 5 个路由 + summary（错误码 400/404/409/503 分类） |
| `app/main.py` | 增加 init_db() 调用 |
| 前端三件套 | 账户概览 5 卡片 + 持仓卡片（编辑/删除）+ 添加/编辑表单（代码自动查名称） |
| `tests/test_calculator.py` | 16 个计算器测试（含精度/除零/舍入） |
| `tests/test_portfolio_api.py` | 15 个 API 测试（内存 SQLite + mock 数据源，无网络） |
| `requirements.txt` | 新增 SQLAlchemy>=2.0.0 |
| `README.md` / `.env.example` | Phase 3 状态、数据库说明、DATABASE_URL 变量 |

### 四、新增 API（均实测通过）

| 接口 | 方法 | 说明 |
|---|---|---|
| `/api/portfolio/holdings` | GET | 全部持仓（含实时计算收益） |
| `/api/portfolio/holdings` | POST | 添加（自动取名；基金不存在 404 / 重复 409 / 数据源 503） |
| `/api/portfolio/holdings/{id}` | GET | 单条持仓 |
| `/api/portfolio/holdings/{id}` | PUT | 修改份额/成本（代码不可改） |
| `/api/portfolio/holdings/{id}` | DELETE | 删除 → `{"success": true}` |
| `/api/portfolio/summary` | GET | 账户汇总（动态计算，不再是全 0） |

### 五、真实数据端到端验收（指令第二十三节数据）

```
添加：000001 / 3000 份 / 成本 1.2000（真实净值 1.3320，2026-09-21）
结果：投入=3600.00  市值=3996.00  收益=396.00  收益率=11.00%  今日估算=-3.00
重复添加 → HTTP 409；汇总接口 → 与单条一致
```

与预期数字完全一致，无硬编码，全部实时计算。

### 六、测试结果

```
pytest tests/ → 53 passed（Phase 1 的 4 + Phase 2 的 19 + Phase 3 的 30）
测试不依赖网络：持仓 API 用内存 SQLite + mock 净值数据源
```

踩坑记录（已修复）：
1. 不能把 sessionmaker 实例直接设为 FastAPI 依赖 override——其 `__call__(**local_kw)`
   签名会被 FastAPI 误解析为必需 query 参数，导致全部请求 422
2. Pydantic 模型字段是 float 时，传入 Decimal 会被静默转 float；
   进入 Decimal 计算边界时统一用 `to_decimal()` 转换

### 七、当前数据库状态

本地库保留了一条验收持仓（000001 / 3000 / 1.2），可在首页直接查看效果，不需要可在前端点删除。

### 八、下一阶段：Phase 4 收益计算完善 / Phase 5 Dashboard（未开始）

Phase 4 计划：基金收益排名、账户收益曲线（需要历史净值序列的市值回放）、基金对账户收益的贡献。
当前 calculator 已具备全部基础计算能力，Phase 4 主要是数据组织与展示。

---

## ✅ Phase 4：基金历史表现分析 + 持仓历史模拟市值（已完成）

### 一、数据源实测结论（写代码前/后发现的问题）

```
接口：东方财富 lsjz（api.fund.eastmoney.com/f10/lsjz）+ startDate/endDate 参数
实测 1：日期区间查询可用，TotalCount 正确（90 天 = 64 条交易日记录）
实测 2（重要坑）：带日期区间参数时，每页固定只返回 20 条，
        pageSize 传 30/49/50/100 全部无效，仍返回 20 条
结论：数据源层按 page_size=20 自动翻页（180 天 123 条需翻 7 页），
     循环终止条件 = 取满 TotalCount 或拿到空页
```

### 二、口径说明（本阶段最重要的红线）

```
基金历史表现   = 基金净值本身在区间的涨跌（区间收益率 + 最大回撤）
历史模拟市值   = 当前持仓份额 × 历史每日净值（模拟回放）

页面与接口文档均已标注：
"历史模拟市值按照当前持仓份额回放历史基金净值，未考虑历史申购、
 赎回、分红及份额变化，因此不代表真实历史账户资产。"
```

### 三、新增/修改文件

| 文件 | 说明 |
|---|---|
| `app/utils/calculator.py` | 新增 3 个函数：calculate_period_return（区间收益，起始 0 除零保护）/ calculate_max_drawdown（基于时间序列峰值回撤）/ calculate_historical_market_value（模拟市值） |
| `app/models/fund.py` | 新增 AnalysisPeriod（int 枚举 7/30/90/180）、NavPoint、FundPerformance |
| `app/models/portfolio.py` | 新增 SimulatedHistoryPoint、SimulatedHistoryResponse（docstring 含口径声明） |
| `app/services/fund_data_source.py` | 新增 get_navs_by_date_range（日期区间 + 自动翻页） |
| `app/services/fund_service.py` | 新增 get_navs_for_period（5 分钟缓存）/ get_fund_performance（名称搜索失败不影响主流程） |
| `app/services/portfolio_service.py` | 新增 get_holding_simulated_history（逐点模拟市值，Decimal 计算） |
| `app/api/fund.py` | 新增 GET /{fund_code}/performance |
| `app/api/portfolio.py` | 新增 GET /holdings/{id}/history |
| `frontend/index.html` | ECharts CDN + 基金历史表现面板 + 模拟市值面板（含免责声明全文）+ 持仓卡片"查看历史"按钮 |
| `frontend/js/app.js` | loadPerformance / loadSimHistory / 图表实例复用 / resize 自适应 / CDN 失败降级文字提示 |
| `frontend/css/style.css` | .chart-box（高 320px）/ .sim-meta / 下拉框宽度 |
| `tests/test_calculator.py` | 新增 8 个 Phase 4 计算测试 |
| `tests/test_performance_api.py` | 新建 10 个 API 测试（MockTransport 全 mock，无网络） |
| `README.md` | Phase 4 状态 + 技术备注 |

### 四、新增 API（均实测通过）

| 接口 | 方法 | 说明 |
|---|---|---|
| `/api/funds/{code}/performance?period=7\|30\|90\|180` | GET | 区间收益率 + 最大回撤 + 净值走势点（正序） |
| `/api/portfolio/holdings/{id}/history?period=7\|30\|90\|180` | GET | 模拟市值逐点回放（份额/投入固定） |

错误码：period 非法 → 422；基金不存在 → 404；数据源异常 → 503；基金代码格式错 → 400。

### 五、真实数据端到端验收（000001 华夏成长混合，2026-09-22 实测）

```
基金历史表现（period=30）：2026-08-24 ~ 2026-09-21，21 个点
  区间收益 +2.70%（净值 1.2970 → 1.3320）  最大回撤 -7.84%（峰值 1.34 → 谷底 1.235）
period=90：64 个点，收益 -11.20%，回撤 -27.00%
period=180：123 个点，收益 +29.19%，回撤 -27.00%
→ 4 个区间的收益与回撤均与脚本独立复算结果完全一致

持仓模拟市值（id=1，3000 份 / 成本 1.2，投入 3600）：
  4 个区间逐点核验：模拟市值 = 3000 × 当日净值，模拟收益 = 市值 - 3600 全部正确
  末点（2026-09-21）：模拟市值 3996.00 / 模拟收益 +396.00（与 Phase 3 当前市值吻合）
```

浏览器验收：历史表现折线图、模拟市值双线图均正常渲染，免责声明完整显示，
控制台无 JS 错误；ECharts CDN 失败时降级为文字提示。

### 六、测试结果

```
pytest tests/ → 72 passed（Phase 1 的 4 + Phase 2 的 19 + Phase 3 的 30 + Phase 4 的 18 + main 1）
API 测试全 mock（MockTransport 同时处理 lsjz 与搜索两种请求），不依赖网络
```

踩坑记录（已修复）：
1. **Pydantic v2 的 Literal[int] 查询参数不可用**：`Literal[7,30,90,180]` 收到
   查询字符串 "30" 时直接 literal_error（Literal 不做 str→int 转换），合法值也报 422。
   改用 `int` 枚举 AnalysisPeriod，可自动转换且非法值仍返回 422。
2. **lsjz 带日期区间时 pageSize 失效**：传 100 实际每页只回 20 条，翻页逻辑
   `len(rows) < page_size` 误判提前终止，导致 90/180 天只拿到 20 条。已改为
   page_size=20 + 取满 TotalCount 才停止。
3. 服务端 5 分钟区间缓存会让修复后的首次验证拿到旧结果（重启服务或等过期即可）。

### 七、下一阶段：Phase 5 Dashboard（未开始）

---

## ✅ Phase 5：专业 Dashboard 改造（已完成）

- **完成日期**：2026-09-22
- **状态**：已完成，72 个 pytest 全部通过，浏览器逐项验收通过
- **目标**：首页从"功能测试页面"升级为真正的个人基金数据终端（信息层级：先看钱 → 收益 → 持仓 → 历史 → 最后深入查基金）

### 一、Dashboard 页面结构（自上而下）

```
顶部导航（深色）：FundPilot AI / Personal Fund Intelligence
                 右侧：最新确认净值日期（不出现"实时"字样，估值数据源已失效）
  ↓
账户总览 5 卡片：总投入 / 当前市值 / 累计收益(+收益率) / 持仓数量 / 盈利持仓数
  ↓
收益趋势：当前持仓历史模拟收益（近 90 天回放），下拉切换持仓基金 + 免责声明全文
  ↓
我的持仓表格：基金/代码/份额/成本价/最新净值/市值/累计收益(金额+收益率)/操作[历史][编辑][删除]
  ↓
基金历史表现：下拉选持仓或手输代码 + [7天][30天][90天][180天] 按钮组
             + 区间收益率/最大回撤/起始净值/最新净值 + 净值走势图
  ↓
基金查询（Phase 2 保留）：代码查详情 / 历史净值表格 / 关键词搜索
  ↓
底部：后端连接状态 + 免责声明
```

无持仓时：总览下方显示空状态引导卡片（"还没有持仓"+ 添加按钮），不显示一排 ¥0。

### 二、文件变更（本阶段只改前端，未新增后端 API）

| 文件 | 说明 |
|---|---|
| `frontend/index.html` | **全量重写**：Dashboard 结构 + 添加/编辑 Modal + 删除确认 Modal + toast 容器 |
| `frontend/css/style.css` | **全量重写**：深色顶栏 / 涨红跌绿(.up/.down) / 表格样式 / Modal / toast / 响应式（900px、640px 断点，手机端表格转卡片） |
| `frontend/js/api.js` | **新增**：fetch 封装 + friendlyError 统一错误文案（不暴露 HTTP 状态码） |
| `frontend/js/charts.js` | **新增**：ECharts 实例复用（init 一次后 setOption）+ resize 自适应 + CDN 失败降级提示 |
| `frontend/js/dashboard.js` | **新增**：全部业务逻辑（总览/表格/趋势/表现/Modal/删除确认/toast） |
| `frontend/js/app.js` | **重写**：工具函数（formatMoney/formatPercent/setColorClass）+ 健康检查 + 入口与事件绑定 |

### 三、API 使用情况（全部复用，0 新增）

| 接口 | 来源 | 用途 |
|---|---|---|
| GET /api/portfolio/summary | Phase 3 | 总览 5 卡片 |
| GET /api/portfolio/holdings | Phase 3 | 持仓表格 + 下拉框填充 |
| GET/POST/PUT/DELETE /api/portfolio/holdings(/{id}) | Phase 3 | 持仓增删改查 |
| GET /api/portfolio/holdings/{id}/history | Phase 4 | 收益趋势图（固定近 90 天） |
| GET /api/funds/{code}/performance | Phase 4 | 历史表现分析（4 区间） |
| GET /api/funds/{code}、/{code}/history、/search | Phase 2 | 基金查询面板 |

### 四、持仓操作交互

- **添加**：Modal 弹窗（代码/份额/成本），输满 6 位代码自动查基金名称，提交后 toast"添加成功"并刷新
- **编辑**：Modal 弹窗，基金代码禁改（换基金 = 删除后重新添加），只改份额/成本
- **删除**：确认弹窗"确定删除「基金名」吗？删除后不可恢复。"，可取消
- **历史**：跳转选中收益趋势图中该基金并平滑滚动到趋势面板

### 五、测试结果

```
pytest tests/ → 72 passed（后端无改动，全部旧测试继续通过）
```

### 六、浏览器验收（指令第二十六节清单，2026-09-22 实测）

- 首页：页面/Header/总投入/市值/累计收益/收益率/持仓数量 全部正常；
  Header 显示"最新确认净值日期：2026-09-21"，全文无"实时"字样
- 持仓：名称/代码/份额/成本/净值/市值/收益/收益率显示正确；
  添加→编辑（代码禁用）→删除（含确认+取消）完整闭环在测试基金 161725 上验证通过，
  验证后已删除，不污染真实持仓
- 图表：收益趋势 canvas 正常；净值走势图正常；7/30/90/180 天切换正常
  （30 天 +2.70%/-7.84%、90 天 -11.20%/-27.00%、180 天 +29.19%/-27.00%，与 Phase 4 独立复算一致）；
  resize 后正常；重复查询不创建多个 ECharts 实例（前后均 1 个 canvas）
- 异常：查询不存在的基金 999999 显示"基金 999999 不存在或没有净值数据"（友好提示非裸状态码）；
  Console 全程无 JS Error
- 未实测 2 项（如实说明）：无持仓空状态（库中有真实持仓不便清空，代码路径已实现）、
  ECharts CDN 真实断网降级（降级代码已实现，逻辑上可达）

### 七、已知问题

- 无持仓空状态与 CDN 断网降级两项未在浏览器实测（原因见上），其余验收项全部通过
- 历史表现与模拟数据仍依赖东财公开网页接口，接口变化风险与 Phase 2 相同

---

## ✅ Phase 6：GPT AI 分析（已完成）

- **完成日期**：2026-09-22
- **状态**：已完成，82 个 pytest 全部通过，真实 AI 端到端验证 + 浏览器验收通过
- **目标**：接入 OpenAI 兼容 API，让 AI 解读后端已计算好的账户数据（只解读，不计算、不交易）

---

### 一、核心设计（红线）

```
后端 Decimal 精确计算（summary / 持仓收益 / 近30天表现 / 市值占比）
        ↓ 全部结构化数据
System Prompt 约束（禁止自行计算数字 / 禁止交易指令 / 约定 JSON 输出）
        ↓
AsyncOpenAI（base_url 可换任意 OpenAI 兼容中转）
        ↓ 强制 JSON 解析（带 ```json 剥壳容错）
今日持仓总结 / 收益来源 / 风险提示 + 固定免责声明
```

- AI 不得自行计算净值或收益：所有数字由后端提供，prompt 中明确禁止编造；
  真实验证中 AI 输出的全部数字（6497.65 市值 / 397.66 收益 / 6.52% / 61.5% 仓位等）与后端计算完全一致
- AI 输出约定 JSON：`today_summary`（字符串）、`profit_sources`（字符串）、
  `risk_warnings`（字符串数组 0~5 条）；解析带剥壳容错，字段缺失给安全默认值而非 500
- 模型输出在前端用 textContent 纯文本渲染，不做 HTML 注入（防 XSS）

### 二、新增/修改文件

| 文件 | 说明 |
|---|---|
| `app/services/ai_service.py` | **重写**：配置状态检查 / 上下文组装（summary+holdings+30天表现+市值占比，Decimal 计算）/ prompt 构造 / AsyncOpenAI 调用（60s 超时，temperature 0.3，max_tokens 1000）/ JSON 解析容错 / 4 类业务异常 |
| `app/models/ai.py` | **新增**：AIAnalysisResponse（model / generated_at / data_date / 三块内容 / 固定 disclaimer） |
| `app/api/ai.py` | **扩展**：POST /api/ai/analysis（异常→状态码映射：未配置 503 / 无持仓 400 / AI 失败或解析失败 502 / 数据源 503） |
| `frontend/index.html` | AI 分析面板（收益趋势与我的持仓之间）：生成按钮 / loading / 三板块结果区 / 面板内错误提示 / 未配置提示 / 固定免责声明 |
| `frontend/js/api.js` | 新增 getAIStatus / runAIAnalysis 封装 |
| `frontend/js/dashboard.js` | checkAIStatus（未配置时禁用按钮并提示）/ runAIAnalysis（按钮 loading→结果/错误，AI 文案不经 friendlyError 映射） |
| `frontend/js/app.js` | 绑定 AI 按钮事件 + 页面加载时检查 AI 配置 |
| `frontend/css/style.css` | .ai-status-tip / .ai-loading（呼吸动画）/ .ai-meta / .ai-section / .ai-risk-list / .ai-error |
| `tests/test_ai_api.py` | **新增** 10 个测试（内存 SQLite + MockTransport 数据源 + mock 模型调用，零网络；含"响应与 prompt 均不泄漏 Key"断言） |
| `tests/manual_test_ai.py` | **新增** 真实端到端验证脚本（真实持仓 + 真实数据源 + 真实 AI API） |
| `.env.example` | **安全修复**：原文件误存真实 API Key，已替换为占位符；真实配置迁入 `.env`（.gitignore 已屏蔽） |
| `README.md` | Phase 6 标注完成 + 实现说明 |

### 三、新增 API（均实测通过）

| 接口 | 方法 | 说明 |
|---|---|---|
| `/api/ai/status` | GET | 配置状态（Phase 1 已建立，本阶段确认保留并验证不泄漏 Key）→ `{"configured": true, "model": "...", "base_url": "..."}` |
| `/api/ai/analysis` | POST | 组装后端数据 → 调用模型 → 返回三块分析内容 + 免责声明 |

错误码：未配置 → **503**；无持仓 → **400**；AI 上游失败 / 返回无法解析 → **502**；数据源异常 → **503**。

### 四、真实端到端验证（2026-09-22 15:55 实测）

```
GET /api/ai/status → configured=true, model=gpt-5.6，响应无 Key
POST /api/ai/analysis（2 条真实持仓，真实净值，gpt-5.6 实际调用 28 秒）→ HTTP 200
  今日持仓总结：总市值 6497.65 / 累计收益 397.66 / 6.52% / 今日估算 16.04 ← 与后端计算完全一致
  收益来源：华夏成长混合 +396.00（11.00%）为主要来源；广发信息技术联接C +1.66（0.07%）
  风险提示：仓位集中度 61.5% / 近30日回撤 -7.84% 与 -6.98% / 广发盈利缓冲有限
  免责声明完整输出
```

### 五、浏览器验收（2026-09-22 实测，8001 端口临时实例）

- 面板位置正确（收益趋势之下、我的持仓之上），按钮可点击，无未配置提示
- 点击后按钮变「AI 分析中...」并禁用，蓝色呼吸加载提示出现；约 60 秒内返回
- 结果区：模型/时间/数据日期元信息正确；三板块内容非空；风险提示为 ul li 列表
- 页面全文无 "sk-" 字符串（Key 泄漏检查通过）；免责声明持续可见；Console 无新增 JS Error
- 注：8000 端口存在一个旧服务实例（启动于 .env 创建之前，AI 显示未配置）。
  无法跨会话终止，重启电脑后用 `python run.py` 正常启动即为最新代码与配置

### 六、测试结果

```
pytest tests/ → 82 passed（Phase 1-5 的 72 + Phase 6 的 10，全 mock 不依赖网络与真实 AI）
真实 AI 端到端：tests/manual_test_ai.py 通过
```

### 七、遗留问题与说明

1. **AI 每日报告未实现**：本阶段指令只要求 AI 分析；日报（定时生成、保存历史）规划在 Phase 8 自动化
2. **AI 输出质量依赖模型**：已用 JSON 约定 + 低温度约束，但"解读正确性"仍以模型能力为限，风险提示不代表专业意见
3. ~~8000 端口旧实例~~ **已解决（2026-09-22 晚）**：根因是 uvicorn `--reload` 模式的
   孤儿子进程——父进程被杀后 worker 子进程残留，继续占用 8000 端口并用旧内存配置响应请求，
   新服务实际绑定失败。解决：`Get-Process python | Stop-Process -Force` 后重启。
   已实测 8000 端口正常返回 `configured:true`，AI 面板在正式端口可用
4. `.env.example` 曾误存真实 Key：本地已替换为占位符；若该文件曾被推送到 GitHub，建议尽快更换 API Key

---

## ✅ Phase 7：智能监控（已完成）

- **完成日期**：2026-09-22
- **状态**：已完成，96 个 pytest 全部通过（原 82 + 新增 14），真实数据验证 + 浏览器验收通过
- **目标**：后端固定规则监控持仓风险并分级提醒；AI 不参与任何数值计算；不做自动交易、不给买卖建议

---

### 一、监控规则（阈值作为常量明确写入 monitor_service.py 顶部）

| 检查项 | 数据口径 | 提示 | 注意 | 高风险 |
|---|---|---|---|---|
| 单日涨跌幅异常 | 数据源最新日涨跌幅（nav_daily_change，取绝对值） | ≥ 2% | ≥ 3% | ≥ 5% |
| 持仓集中度过高 | 单只基金市值 ÷ 账户总市值 | ≥ 40% | ≥ 60% | ≥ 80% |
| 历史最大回撤 | 单只基金近 90 天净值峰值回撤（复用 calculate_max_drawdown） | ≥ 5% | ≥ 10% | ≥ 20% |
| 近期异常波动 | 近 30 天内 \|日涨跌幅\| ≥ 2% 的交易日数量 | ≥ 2 天 | ≥ 3 天 | ≥ 5 天 |

- 一条提醒只取最高达到档位；返回前按 高风险 → 注意 → 提示 排序（同级按基金代码）
- 每条提醒含：type/type_label、level/level_label、fund_code/fund_name、reason（触发原因）、detail（具体数据）
- 无持仓 → summary="暂无持仓，添加持仓后即可启用智能监控"；无异常 → summary="当前未发现明显异常"
- 数据不足友好处理（不伪造数据）：波动窗口内有效日涨跌幅样本 < 5 → 跳过波动检查；
  近 90 天有效净值点 < 3 → 跳过回撤检查；区间净值接口单只失败 → 跳过该只的回撤/波动检查；
  以上全部写入 data_issues 如实说明。数据源整体不可用 → 503（与 AI 路由同约定）
- 口径说明：回撤当前为"单只基金近 90 天净值回撤"（复用 Phase 4 计算器）；
  README 早期设想的"账户累计回撤"为账户级聚合口径，留待后续阶段

### 二、新增/修改文件

| 文件 | 说明 |
|---|---|
| `app/models/monitor.py` | **新增** MonitorAlert（type/level/基金/原因/detail）/ MonitorResponse（generated_at/data_date/checked_count/alert_count/summary/alerts/data_issues/固定免责声明） |
| `app/services/monitor_service.py` | **新增** 四类规则检查 + `_tier` 三级分档 + 排序 + data_issues 收集；阈值常量在文件顶部 |
| `app/api/monitor.py` | **新增** GET /api/monitor（DataSourceUnavailableError/FundNotFoundError → 503） |
| `app/main.py` | 注册 monitor 路由 |
| `frontend/index.html` | 智能监控面板（账户总览之下、收益趋势之上）：重新检查按钮 / loading / 提醒列表 / 无异常提示 / 数据问题说明 / 固定免责声明 |
| `frontend/js/api.js` | 新增 getMonitor |
| `frontend/js/dashboard.js` | loadMonitor（自动+手动触发）/ buildMonitorItem（createElement+textContent 纯文本渲染防注入） |
| `frontend/js/app.js` | 页面加载自动检查 + 绑定"重新检查"按钮 |
| `frontend/css/style.css` | .monitor-item（左侧竖线随等级变色）/ .level-badge（红/橙/蓝）/ .monitor-all-clear（绿）/ .monitor-issues（黄） |
| `tests/test_monitor_api.py` | **新增** 14 个测试（内存 SQLite + MockTransport 全 mock；mock 净值日期基于 date.today() 动态生成，避免波动窗口随时间漂移导致测试过期） |

### 三、新增 API

| 接口 | 方法 | 说明 |
|---|---|---|
| `/api/monitor` | GET | 执行四类规则检查，返回提醒列表（等级排序）+ 数据问题说明 + 免责声明 |

错误码：数据源整体不可用 → **503**；无持仓/无异常 → 200（summary 区分）。

### 四、真实数据验证（2 条真实持仓，2026-09-22 晚实测）

```
GET /api/monitor → HTTP 200，5 条提醒：
  华夏成长混合：近90天回撤 -27.00%（高风险）/ 近30天7个交易日波动超2%（高风险）
  广发信息技术联接C：近90天回撤 -30.79%（高风险）/ 近30天7个交易日波动超2%（高风险）
  华夏成长混合：市值占比 61.32%（注意，≥60% 档）
当日单日涨跌幅无异常未触发；data_date=2026-09-22；排序正确（danger → warning）
```

### 五、浏览器验收（2026-09-22 晚实测）

- 面板位置正确（账户总览之下、收益趋势之上），5 条提醒完整渲染
- 等级徽章红（高风险）/橙（注意）显示正确，条目含类型/基金/原因/数据
- 免责声明完整展示（含"不构成投资建议""自动交易"）
- "重新检查"按钮点击后正常重载，无 loading 残留、无失败提示
- Console 无 JS 异常（无 TypeError/未捕获异常）

### 六、测试结果

```
pytest tests/ → 96 passed（Phase 1-6 的 82 + Phase 7 的 14，全 mock 不依赖网络）
覆盖：无持仓 / 全部正常 / 涨跌幅三级（-3.5% 注意、+5.2% 高风险、温和不触发）/
集中度（单只 100% 高风险、三只等值不触发）/ 回撤 -22% 高风险且波动不误报 /
波动 3 天注意级 / 净值点不足跳过 / 区间接口单只失败降级 / 数据源整体不可用 503 / 等级排序
```

### 七、遗留问题

1. 账户级聚合回撤未实现（当前为单只基金口径），留待后续按需求补充
2. 监控为页面加载/手动触发，定时自动监控与通知规划在 Phase 8
3. GPT 解读监控事件的整合（规则触发 → AI 解释）规划在 Phase 8（AI 每日报告）

---

## ✅ Phase 8：自动化（2026-09-23 完成）

### 一、交付内容

后台定时任务：每轮自动执行 **智能监控 → 保存快照 → 生成 AI 每日报告**。

1. 定时机制：asyncio 协程后台循环（未引入新依赖），FastAPI lifespan 中启动 / 停止，
   不阻塞主线程；"先睡一个间隔再跑第一轮"（sleep-first），避免启动瞬间请求外部接口
2. 快照落库：新表 `monitor_snapshots`（executed_at / checked_count / alert_count /
   summary / alerts_json / data_issues_json），提醒结构复用 Phase 7 的 MonitorAlert
3. AI 日报：新表 `daily_reports`（report_date 唯一 / generated_at / model / data_date /
   content_json / disclaimer），**完全复用 ai_service.run_analysis**（同样的数据组装、
   prompt、模型调用、JSON 剥壳解析、免责声明），每天最多一份（唯一约束 + 生成前查询守护）
4. 失败隔离（指令 7）：监控失败 → 保存"失败记录"快照（data_issues 注明，不伪造数据）；
   日报失败（未配置 / 无持仓 / 上游 / 解析）→ 只记 errors；落库失败 → 记 errors；
   任何异常不逃出任务循环，下一轮继续
5. 配置化（指令 9）：`AUTO_TASK_ENABLED`（默认 true）/ `AUTO_MONITOR_INTERVAL_MINUTES`
   （默认 60，最小 1，非法值回退默认）/ `AUTO_DAILY_REPORT_ENABLED`（默认 true），
   已写入 .env 与 .env.example

### 二、新增接口（app/api/automation.py）

| 接口 | 说明 |
|---|---|
| GET /api/auto/latest | 最近一次快照（未执行过 → 200 null，前端显示引导） |
| GET /api/auto/daily-report | 最新日报（没有 → 200 null） |
| POST /api/auto/run | 手动触发一轮（与定时任务同一 run_auto_check，失败也返回 200+errors） |

### 三、修改/新增文件

```
新增  app/services/automation_service.py   配置解析/快照/日报/run_auto_check/循环/启停
新增  app/api/automation.py                三个路由
新增  app/models/automation.py             Pydantic 出入参模型
新增  tests/test_automation_api.py         12 个测试
修改  app/database/models.py              新增 MonitorSnapshot / DailyReport 两张表
修改  app/main.py                          lifespan（启动/停止调度器）+ 注册 automation 路由
修改  frontend/index.html                 「最近一次自动检查」「AI 每日报告」两个面板
修改  frontend/js/api.js                  getAutoLatest / getDailyReport
修改  frontend/js/dashboard.js            loadAutoCheck / loadDailyReport（textContent 防注入）
修改  frontend/js/app.js                  DOMContentLoaded 加载两个面板
修改  .env / .env.example                 AUTO_* 三个配置项
修改  README.md                           「7. 自动化」章节重写 + Phase 8 标注完成
```

### 四、真实数据验证（2026-09-23 上午实测）

```
POST /api/auto/run（第 1 轮）→ errors 为空
  snapshot：executed_at=2026-09-23 08:03:11，checked=2，alerts=5
    华夏成长混合：回撤 -27.00% 高风险 / 波动 7 天 高风险
    广发信息技术联接C：回撤 -30.79% 高风险 / 波动 7 天 高风险
    华夏成长混合：集中度 61.32% 注意
  daily_report_generated=True：report_date=2026-09-23，model=gpt-5.6，
    内容引用后端数据（总市值 6492.24 元、累计收益 392.25 元、收益率 6.43%）
POST /api/auto/run（第 2 轮）→ daily_report_generated=False（每天一份守护生效），errors 为空
```

### 五、浏览器验收（2026-09-23 上午，6 项全过）

- 页面正常加载，Console 无 JS 异常
- 「最近一次自动检查」面板（智能监控之下）：执行时间/持仓数/提醒数元信息 + 5 条提醒带等级徽章
- 「AI 每日报告」面板（AI 分析之下）：报告日期/生成时间/模型 + 总结/收益来源正文 + 免责声明
- Phase 7 智能监控面板与原有面板（总览/持仓）不受影响
- 面板截图已核对（两张）

### 六、测试结果

```
pytest tests/ → 108 passed（Phase 1-7 的 96 + Phase 8 的 12，全 mock 不依赖网络）
覆盖：快照/日报查询（空 → null）、手动触发全流程、每天一份守护、快照排序、
AI 失败不崩溃、日报开关、数据源失败保存失败快照、配置解析（默认/非法/钳制/开关）、
start/stop 生命周期、lifespan 集成
```

### 七、遗留问题

1. ~~首轮自动检查要等一个间隔（默认 60 分钟）才执行~~ → 已在 Phase 9 优化：启动后约 5 秒执行首轮
2. 快照无保留上限（每轮一条，量级很小，暂不清理）；无"提醒变化趋势"展示
3. AI 日报失败只记 errors/日志，不重试（下一轮自动补生成）
4. 账户级聚合回撤仍未实现（Phase 7 遗留）


## ✅ Phase 9：Web AI API 配置 + 安全本地持久化（2026-09-23 完成）

### 一、交付内容

Dashboard 新增「AI API 配置」面板（AI 每日报告之后），Web 配置优先于 .env，Key 加密落库。

1. **加密方案**：`cryptography` 的 Fernet 对称加密；密钥由环境变量
   `FUND_PILOT_SECRET_KEY`（.env 中已生成随机值）做 sha256 派生，**密钥本身不落任何文件代码**；
   未配置该变量时后端拒绝保存（400 明确提示），前端显示黄色警告
2. **存储**：新表 `web_ai_configs`（单行 upsert）：`encrypted_api_key`（Fernet 密文）/ base_url /
   model（明文，非敏感）/ updated_at；删除该行即"清除 Web 配置"
3. **优先级**：Web 行存在则逐字段优先，字段为空回落 .env；Key 解密失败（如换了 secret）时
   忽略 Web Key 回落 .env 并记日志；清除后完全恢复 .env
4. **脱敏**：所有 API 响应 / 页面只显示"前 3 位 + **** + 后 4 位"；测试连接失败信息会先
   把 Key 替换为 `***` 再返回；日志绝不输出 Key
5. **测试连接**：最小化请求（"请只回复 OK"，max_tokens=10），面板免责声明注明消耗少量 Token
6. **兼容性**：ai_service.run_analysis / _call_model 对外签名不变（_call_model 增加可选 config
   参数），Phase 6/7/8 全部测试无需改动即通过；不改基金数据逻辑，无自动交易

### 二、新增接口（app/api/ai.py）

| 接口 | 说明 |
|---|---|
| GET /api/ai/config | 生效配置状态（来源 web/env、脱敏 Key、secret_key_ready） |
| PUT /api/ai/config | 保存 Web 配置（Key 加密；无 FUND_PILOT_SECRET_KEY → 400） |
| DELETE /api/ai/config | 清除 Web 配置（回退 .env） |
| POST /api/ai/config/test | 最小化请求测试连接（留空字段用生效配置） |

### 三、修改/新增文件

```
新增  app/services/web_config_service.py    加密存取/脱敏/优先级解析（effective config）
新增  tests/test_ai_config_api.py           10 个测试
新增  tests/conftest.py                     autouse：测试期间禁用 AUTO_TASK_ENABLED（防后台轮次污染真实库）
修改  app/database/models.py                新增 WebAiConfig 表
修改  app/services/ai_service.py            get_ai_status/effective config/_call_model 抹 Key/新增 test_connection
修改  app/services/automation_service.py    首轮启动后 5 秒执行（FIRST_RUN_DELAY_SECONDS，不等完整间隔）
修改  app/api/ai.py                         配置四个路由（/status 也改走生效配置）
修改  app/models/ai.py                      WebAIConfigIn / AIConfigTestIn / AIConfigTestResult
修改  frontend/index.html                   「AI API 配置」面板 + 监控/自动检查面板折叠按钮
修改  frontend/js/api.js                    getAIConfig / saveAIConfig / clearAIConfig / testAIConfig
修改  frontend/js/dashboard.js              loadAIConfig / save / test / clear + 折叠联动提示文案
修改  frontend/js/app.js                    配置按钮绑定 + .collapse-btn 通用折叠逻辑
修改  frontend/css/style.css                .head-actions / .panel.collapsed / .config-grid
修改  .env                                  新增 FUND_PILOT_SECRET_KEY（随机 64 位 hex，本机生成）
修改  .env.example                          FUND_PILOT_SECRET_KEY 占位（无真实密钥）
修改  requirements.txt                      cryptography>=42.0.0
```

### 四、测试结果

```
pytest tests/ → 118 passed（Phase 1-8 的 108 + Phase 9 的 10，全 mock 不依赖网络）
新增覆盖：无 secret 禁保存(400)、数据库无明文+可解密还原、响应只含脱敏 Key、
重启恢复（新会话+重新派生密钥解密）、Web 优先/字段级回退、清除恢复 .env、
测试连接成功（max_tokens≤20）/失败（错误信息抹 Key）/配置不全(400)、首轮快速执行
```

### 五、浏览器验收（2026-09-23 实测，7 项全过）

- 「AI API 配置」面板渲染正常，状态行"已配置（来源：.env 配置 · Key sk-****SQJD）· 模型：gpt-5.6"，
  全页搜索完整 Key 无泄露
- 「智能监控」「最近一次自动检查」折叠按钮：正文收起/展开正常，按钮文字随之切换
- 保存假配置 → 状态行变"来源：Web 配置 · Key sk-****6789"；测试连接（假地址）→"连接失败"且无明文 Key
- 清除 Web 配置 → 恢复"来源：.env 配置"；测试连接（真实配置）→"连接成功（本次测试消耗了少量 Token）"

### 六、遗留问题

1. 换机/换 FUND_PILOT_SECRET_KEY 后，已存 Web Key 无法解密（自动忽略回落 .env，需重新在页面保存一次）
2. Web 配置仅存本机 SQLite，无多用户隔离（个人项目定位，不做鉴权）
3. 加密为对称 Fernet，防"直接翻数据库"，不防本机管理员读取 .env（与本机独有密钥的要求一致）

---

## ✅ Phase 10：项目工程化与 GitHub 发布准备（2026-09-23 完成）

1. **目录清理**：旧阶段指令 第四阶段.txt / 第五阶段.txt 移入 `docs/dev-notes/`；
   删除 `.pytest_cache/` 与所有 `__pycache__/`；新建 `docs/screenshots/`（含 .gitkeep，README 约定截图放这里）
2. **.gitignore 补充**：新增 `.pytest_cache/`、`*.log`、`.trae/`、`data/*.db-journal`
3. **安全扫描**：全仓 grep 真实 Key（sk-jHmP…）→ 仅存在于 .env（已 gitignore）；
   代码 / README / 测试 / docs 无硬编码密钥；.env.example 仅占位符
4. **bug 修复**：折叠按钮只藏了小字备注、卡片还在——原因是 JS 设置的内联
   `display:block` 优先级高于折叠 CSS 规则，改为 `display: none !important` 后修复
5. **新用户场景冒烟**（全新临时库 + 清空 AI 配置）：自动建表、无持仓 summary、
   AI 未配置 status/分析 503、无 secret 保存 400、无持仓监控友好文案、无快照/日报返回 null
   —— 8 项全部符合预期
6. 测试 118 passed；浏览器回归 7 项全过（重点：两个面板折叠时卡片也正确隐藏）

遗留：`web_ai_configs` 表中有 1 行用户保存的配置（与 .env 同值，加密存储，属正常使用）；
tests/manual_test_*.py 两个手动冒烟脚本保留（不参与 pytest，供真网联调用）。

---

## ✅ Phase 11：最终产品体验与 GitHub 发布准备（2026-09-23 完成）

交付内容：

1. **产品名称统一**：全仓 grep fundpilot 残留 10 处均为合理保留（开发进度.md 历史记录、
   data/fundpilot.db 数据文件名及其注释说明）；对外展示名称全部为「钱程似锦 Qiancheng」
2. **UI 一致性**：字体/间距/按钮/卡片/颜色（主题色 #4c6ef5、涨红跌绿）/错误提示体系统一；
   修复唯一不一致点——「基金查询」面板 h2 补上 panel-head 包裹（frontend/index.html）
3. **文案检查**：Loading（正在加载.../正在分析...）、空状态（还没有持仓/暂无数据/当前未发现明显异常）、
   错误（friendlyError 统一映射 + 面板内长文案）、成功 toast 全部风格一致，无需修改
4. **响应式**：断点 900/720/640 完整（卡片 3→2 列、config-grid 单列、持仓表格转卡片式），
   CSS 规则浏览器端确认已加载（9 条 max-width 规则）
5. **安全复查**：真实 Key 仅在 .env（gitignored）；README 仅占位符；无 .log 文件；
   data/ 仅 fundpilot.db + .gitkeep；git 尚未 init，.gitignore 规则覆盖 .env/data/缓存/IDE 完备
6. **5 张脱敏截图**（docs/screenshots/，虚拟份额成本 + 临时库 + AI Base URL 用通用地址 +
   Key 天然脱敏）：dashboard.png / monitor.png / auto-report.png / ai-config.png / ai-analysis.png
   （AI 分析为临时库演示持仓的真实 AI 调用结果）；截图后临时库、启动脚本、进程已全部清理
7. **README**：截图章节改为实际图片展示（5 张嵌入 + 脱敏说明）；新增「🛠 核心技术亮点」
   章节（Decimal 精确计算 / FastAPI async / Fernet 加密 / asyncio 自动化 / 固定规则风控 /
   textContent 防注入 / 118 项 pytest）
8. **测试**：pytest 118 passed；8000 正式端口浏览器回归全部 PASS（真实持仓总览、监控重新检查、
   两面板折叠/展开、AI 配置脱敏只读、当日日报显示、响应式规则加载）

截图过程踩坑（重要）：

- **8001 端口残留进程**：Phase 10 冒烟测试的 `--env-file smoke.env` 实例进程未退出，
  Windows 下与截图实例"双绑定" 8001 端口导致请求路由漂移（截图一度拿到空库数据）。
  排查：`Get-NetTCPConnection -LocalPort 8001 | Select OwningProcess` + Win32_Process.CommandLine。
  教训：**临时端口实例用完必须 Stop-Process**，且验证服务身份要看进程命令行而非仅 health OK
- **后台 job 进程被 agent 会话波及**：browser agent 会话结束会连带终止工具宿主托管的
  uvicorn 后台进程；`Start-Process` 独立进程寿命也不稳定。最稳做法：run_in_background 启动 +
  一轮 agent 内连续完成截图
- **Start-Process 不继承 shell 临时环境变量**（本会话实测），DATABASE_URL 等配置要写进
  启动脚本（脚本在 data/ 下需手动 sys.path.insert 项目根）；脚本路径含空格时
  ArgumentList 需手动加引号
- browser_console_messages 会读到浏览器 profile 累积的历史日志（含已关闭页面的报错），
  判断当前页面是否有错应以服务端日志为准

遗留问题：

1. git 尚未 init（发布时执行 git init → 首次 commit，.gitignore 已完备）
2. README 中 GitHub 仓库地址为占位符（`你的用户名/Qiancheng`），init 后需替换真实用户名
3. 浏览器若装有 Vite 开发者扩展会请求 /@vite/client 产生 404（与应用无关，无害）

### Phase 11 复验与补全（2026-09-23 下午，新会话）

复验中发现并修复 2 个产品问题：

1. **无持仓时总览卡显示一排 ¥0.00**（与 index.html「空状态不显示一排 ¥0」的设计意图不符）
   → `dashboard.js` loadHoldings 空持仓分支隐藏 `#summary-cards`，有持仓时恢复显示；
   空库冒烟环境复验通过，真实库回归确认有持仓时卡片正常
2. **AI API 配置面板密钥未就绪提示未套用统一黄色警告样式**
   → `style.css` 的 `.ai-config-warn` 复用 `.ai-status-tip` 样式

复验结论（全部通过）：

- 桌面端空状态 8 项：空状态引导卡 / 智能监控友好提示 / AI 按钮禁用+黄条 / 配置面板密钥警告 /
  自动检查与 AI 日报空状态文案 / 无横向溢出（scrollWidth 差 0）
- 手机端 375px：断点规则（900/720/640）+ 真实 375px 宽截图验证（卡片两列、配置输入单列、
  表格转卡片、无溢出、导航正常）
- 异常场景（空库 + AI 未配置 + 无 FUND_PILOT_SECRET_KEY 冒烟环境）：
  AI 分析 503 友好文案 / PUT 配置 400 明确提示 / 基金代码格式 400 / 不存在基金 404
- pytest 118 passed；真实库最终回归 6 项 ALL PASS（总览卡可见 / 持仓行数与 API 一致 /
  监控渲染 / AI 按钮可用 / 无溢出 / 监控面板折叠展开）
- **5 张截图重制为脱敏版**：此前版本混入了真实数据，已全部替换——临时库 shots.db +
  3 只虚拟持仓（000001/002974/161725，其中一只占比 89.21% 触发集中度高风险演示）+
  Web 配置保存假 Key（sk-TESTDEMO123456，页面天然只显示 sk-****3456 脱敏形式）；
  AI 分析与 AI 每日报告为临时库环境下的真实 AI 调用结果
- 临时库 / 临时 env 文件已删除；`.gitignore` 补充 `smoke.env`、`shots.env`

环境排查重要发现（非产品 bug，但直接影响本机调试，务必记住）：

- **系统代理劫持本机请求**：本机配置了 HTTP(S)_PROXY，httpx 等默认 trust_env 会把
  127.0.0.1 请求发往代理，造成超时 / 空响应 / 数据"漂移"假象。调试本机服务一律
  `trust_env=False`（httpx）、`--no-proxy-server`（浏览器）、curl.exe
- **子进程 stdout=PIPE 不读取会死锁**：uvicorn 日志写满管道缓冲区（约 8KB）后整个
  事件循环冻结，表现为接口无限挂起（曾被误判为 monitor 死锁）。长驻子进程日志应重定向到文件
- **ECharts CDN（jsdelivr）会阻塞 networkidle**：页面加载判断用 domcontentloaded +
  等待业务 DOM 标记（如 .holdings-table tbody tr、.monitor-item），不要等 networkidle
- **工具宿主会周期回收后台进程（约 2-4 分钟）**，Start-Process 脱离托管也难幸免；
  可靠做法是把"启动→验证→操作"压进同一条命令，或用 playwright 脚本在进程内托管服务
- **8001 端口曾反复出现不明 uvicorn 实例（读真实 .env）抢占端口**：临时服务一律改用
  8002，且每次操作前用 API 数据特征（如持仓数量）确认连的是哪个库

遗留问题（新增 1 条）：

4. .venv 新增 playwright + py-spy（仅开发调试用，未写入 requirements.txt；
   如不需要可 `pip uninstall playwright py-spy`）

---

## ✅ Phase 12：投资决策辅助（2026-09-23 完成）

### 一、交付内容

1. **投资目标设置**：目标收益率可保存/修改/持久化（`investment_goals` 表单行 upsert，
   校验 0 < x ≤ 500），存库后重启不丢
2. **目标收益分析**：当前收益率 / 目标收益 / 距离目标（百分点）/ 是否已达到目标（仅状态，
   不生成卖出结论）/ 账户级近 180 天回撤 / 近 30 天大波动天数 + 分持仓明细表，
   全部后端 Decimal + ROUND_HALF_UP 计算
3. **基金关注（候选池）**：东财公开排行（股票型 / 混合型各取近 1 年收益前 15）合并去重，
   逐只用近 180 天公开净值校验区间收益 / 最大回撤 / 30 天波动，历史不足的剔除并写入
   `data_issues`；统一定位为「符合当前量化筛选条件的候选基金」，面板明示"不代表任何推荐"
4. **AI 目标结论 + AI 候选池解读**：各自专属 System Prompt；结论枚举三选一
   （接近目标 / 已达到目标 / 风险需要关注），候选池解读只解释"为什么符合当前筛选条件"
5. **原「智能监控」改名「风险详情」**：默认折叠弱化展示（普通用户看不懂的技术指标收起来），
   展开交互正常
6. **全局醒目免责声明**：GLOBAL_DISCLAIMER 横幅置于首页最顶部（黄色警示样式）：
   "以上内容基于历史数据及模型分析，仅用于投资决策辅助，不代表未来收益，不构成投资建议。
   基金投资有风险，过往表现不代表未来表现。系统不会自动进行任何交易。"
7. **首页聚焦**：我的目标收益 → 当前收益 → 距离目标 → 系统分析（进度条 + 链路文案），
   让普通用户一眼看懂"赚多少、离目标还有多少、该关注什么"

### 二、用户 3 项调整 + AI 红线的落地（重要约束，后续勿回退）

1. **AI 不输出交易指令**：目标结论枚举仅 `near_target / target_achieved / risk_attention`
   （ai_service.GOAL_CONCLUSION_ENUMS + 中文标签映射）；System Prompt 明令禁止
   "应该买 / 应该卖 / 建议止盈 / 加仓 / 减仓 / 赎回"；模型输出非法枚举 → 回落
   `risk_attention` 并置 `low_confidence=true`
2. **候选池不是推荐**：全文禁止"推荐购买 / 值得买 / 最优 / 必涨 / 一定盈利"等表述；
   AI 只解释每只基金为什么符合当前量化筛选条件（引用该基金提供的数据）
3. **目标收益 ≠ 止盈条件**：达到目标只显示"已达到目标"状态及数据依据，绝不自动生成
   卖出结论；页面同时展示当前收益 / 目标 / 距离 / 历史表现 / 回撤 / 波动及影响因素
4. **候选池 AI 红线（解析层强制，不只是 prompt 约束）**：
   `FORBIDDEN_PREDICTIVE_PHRASES`（有望上涨 / 预计会继续上涨 / 盈利概率 / 未来表现较好 /
   值得买 / 推荐购买 / 建议买入 …）——`overview / market_background` 命中 → 整篇拒绝
   （AIResponseParseError → 502）；`highlights.reason / cautions` 命中 → 逐条丢弃。
   历史数据只用于描述和解释，不用于预测未来收益

### 三、新增/修改文件

后端：
- `app/database/models.py`：+InvestmentGoal（表名 `investment_goals`，单行）
- `app/models/goal.py`（新）：GoalSaveIn / GoalResponse / GoalHoldingItem / GoalAccountInfo / GoalAnalysisResponse
- `app/models/candidate.py`（新）：CandidateItem / CandidatePoolResponse
- `app/models/fund.py`：+FundRankingItem / FundRankingPage
- `app/models/ai.py`：+GoalConclusionResponse / CandidateHighlight / CandidateAnalysisResponse
- `app/services/goal_service.py`（新）：目标 CRUD + build_goal_analysis（Decimal 计算）+ GLOBAL_DISCLAIMER
- `app/services/candidate_service.py`（新）：量化筛选 + 逐只 enrich（180 天净值校验）
- `app/services/fund_data_source.py`：+get_fund_ranking（JSONP 剥壳，见"六、真实 API 踩坑"）
- `app/services/fund_service.py`：+排行数据缓存
- `app/services/ai_service.py`：GOAL_SYSTEM_PROMPT / CANDIDATE_SYSTEM_PROMPT /
  FORBIDDEN_PREDICTIVE_PHRASES + 解析层红线 + run_goal_conclusion / run_candidate_analysis
- `app/api/goal.py`（新）：GET/PUT `/api/goal`、GET `/api/goal/analysis`
- `app/api/ai.py`：+POST `/api/ai/goal-conclusion`、POST `/api/ai/candidate-analysis`
  （注意必须注入 db，见"六、真实 API 踩坑"第 3 条）
- `app/api/fund.py`：+GET `/api/funds/candidates`（必须注册在 `/{fund_code}` 之前）
- `app/main.py`：注册 goal 路由

前端：
- `frontend/index.html`：全局免责横幅 + 投资目标面板 + 基金关注面板 + 监控面板改名并默认折叠
- `frontend/js/app.js`：loadGoal() 初始化 + 目标保存/AI 结论/候选池加载/AI 解读 4 个按钮绑定
- `frontend/js/dashboard.js`：目标渲染（链路文案 / 进度条 / 指标卡 / 分持仓明细）+ 候选池渲染
  （td 带 data-label 供手机端卡片布局）
- `frontend/css/style.css`：.global-disclaimer / .goal-rate-input / .goal-link-text /
  .progress-track / .progress-fill / .goal-metrics / .goal-conclusion-box / .ai-banner / 640px 适配

测试：
- `tests/test_goal_api.py`（9）、`tests/test_candidates_api.py`（6）、`tests/test_ai_goal.py`（10）

### 四、新增 API（均已实测通过）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET/PUT | `/api/goal` | 读取 / 保存目标收益率（单行 upsert） |
| GET | `/api/goal/analysis` | 目标收益分析（含未设置目标的 200 降级引导） |
| GET | `/api/funds/candidates` | 候选池（count 5-30，默认 20；数据源异常 503） |
| POST | `/api/ai/goal-conclusion` | AI 目标结论（枚举三选一，未配置 503 / 上游 502） |
| POST | `/api/ai/candidate-analysis` | AI 候选池解读（红线解析层强制） |

### 五、测试与浏览器验收

- **pytest：143 passed**（118 旧 + 25 新：目标 API 9 + 候选池 API 6 + AI 目标/候选池 10）
- **浏览器验收（真实库 + 真实 AI gpt-5.6，2026-09-23）**：
  - 桌面 1280 + 手机 375 共 15 项：免责横幅置顶（y=92）、目标链路文案
    "目标收益 10.00% · 当前收益 +6.43% · 还差 3.57 个百分点"、进度条 64.3%（后端计算）、
    AI 结论标签合规且无交易指令、风险详情默认折叠可展开、手机端无横向溢出、
    目标指标卡手机端降两列（此前 14/15，唯一失败为候选池真实 API bug，修复后如下）
  - **候选池聚焦复验（rankhandler 修复后）**：真实加载 20 只候选（无 503）、
    元信息与筛选规则可见、表头指标齐全、AI 解读真实调用渲染成功（overview + 入选原因 5 条）、
    **红线禁词零命中**、面板免责声明可见
  - **目标面板补验（显式等待）4/4**：链路文案 / 进度条 / 清理复核全过
- 回归期间写入真实库的目标行已清理（investment_goals count=0，真实持仓 2 行完好）

### 六、真实 API 踩坑（重要，勿重蹈）

1. **rankhandler.aspx 响应不是严格 JSON**：形如 `var rankData = {datas:[...],allRecords:N};`
   ——键名无引号的 JS 对象字面量，`json.loads` 直接报错；需先
   `re.sub(r'([{,]\s*)([A-Za-z_]\w*)\s*:', r'\1"\2":', ...)` 补引号再解析
2. **st=fd 是升序（最差基金排前面）**！近 1 年收益降序要用 `sc=1nzf&st=desc`
   （2026-09-23 逐参数实测确认）；datas 每行字段索引：0=代码 1=名称 3=净值日期
   4=单位净值 6=日涨幅 9=近3月 10=近6月 11=近1年
3. **AI 路由必须注入 db**：`run_candidate_analysis(candidates, db)`——
   `web_config_service.get_effective_ai_config(db=None)` 会自建 SessionLocal 读**真实库**，
   测试不注入会拿到用户真实 Web 配置（模型名/已配置状态断言全错）
4. mock 测试的 rank_jsonp 要忠实还原**无引号键名**格式（带引号碰巧也能解析但不忠实）
5. 候选池两步校验：排行接口取"近 1 年前 15"只是初筛，逐只再拉 180 天净值算区间收益 /
   回撤 / 波动，历史不足 180 天的剔除并如实写入 data_issues，绝不伪造

### 七、遗留问题

1. 候选池依赖东财公开排行接口，接口变动会导致 503（已有友好降级文案与 data_issues 说明）
2. AI 候选池解读耗时约 20-60 秒，前端仅按钮禁用态、无进度提示（可接受但可优化）
3. 免责声明覆盖页面展示场景；项目无导出/分享功能，暂无其他场景需要覆盖

---

## ✅ Phase 13：候选池增强 + AI 体验优化（2026-09-23 完成）

### 一、交付内容

1. **候选池筛选条件（存库）**：`candidate_filters` 表（单行 id=1 upsert，复刻 investment_goals
   模式）；可调维度仅两个——基金类型勾选（股票型/混合型，至少勾一类）+ 近 1 年收益下限
   （-100~1000，0=不过滤）；`is_set=false` 表示从未保存过（默认条件 = 全勾 + 0，行为与
   Phase 12 完全一致）
2. **后端过滤管线**：按勾选决定排行请求类别（仅勾一类只发一次请求）→ 合并去重 →
   先类型显式过滤再按近 1 年收益下限过滤（发生在历史校验之前，省数据源请求）→
   `[:count]` 截断；**不足 count 如实返回少量结果，绝不从最差补位**；被过滤数量记入
   data_issues 汇总说明
3. **SCREENING_RULE 动态化**：`build_screening_rule()` 按用户条件生成规则文案
   （保留"不构成任何推荐"整句；文案本身无任何推荐/预测表述）
4. **筛选条件透明展示**：候选池面板新增筛选区块（类型勾选 + 收益下限输入 + 保存按钮），
   `#filter-desc` 显示当前生效条件（初始化回显 / 加载候选池后用后端返回的规则文案）
5. **AI 候选池 Loading 优化**：点击 AI 解读显示
   「正在分析 X 只候选基金，请稍候（约需 20~60 秒，AI 只解读历史数据）...」
   （X = 已加载候选数，ai-loading 动画复用），完成后隐藏；不再改按钮文字
6. **目标分析 5 状态**（后端 `classify_goal_status` 互斥单选，阈值常量写在
   goal_service.py 顶部）：优先级 未设置 > 风险需要关注（90 天回撤 ≥10% 或 30 天大波动
   ≥3 天；两项均无数据不触发）> 已达到目标（进度 ≥100%）> 接近目标（≥50%）>
   距离目标较远（<50%）；前端目标面板进度条头部显示状态徽章
   （risk 红 / achieved 蓝 / near 橙 / far·not_set 灰）；**只描述状态，无任何交易指令**，
   AI 三枚举结论机制不变（AI 结论与状态徽章是两回事）

### 二、新增 API

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/candidates/filter` | 当前筛选条件（未保存过 → 默认 + is_set=false） |
| PUT | `/api/candidates/filter` | 保存条件（全不勾 → 422；越界 → 422） |

`GET /api/funds/candidates` 行为变化：签名加 db 依赖，条件从库读取（不加 Query 参数）；
`/api/ai/candidate-analysis` 内部同样读库条件（`get_candidates(20, db)`）。

### 三、新增/修改文件

后端：
- `app/database/models.py`：+CandidateFilter（candidate_filters 表）
- `app/models/candidate.py`：+CandidateFilterSaveIn（model_validator 至少勾一类）/
  CandidateFilterResponse
- `app/models/goal.py`：GoalAnalysisResponse +status/status_label
- `app/services/candidate_service.py`：删硬编码 SCREENING_RULE；+FUND_TYPE_STOCK/MIXED、
  DEFAULT_MIN_RETURN_1Y、FT_TO_TYPE、build_screening_rule、get_filter、save_filter；
  get_candidates(count, db) 过滤管线重构（默认条件行为不变）
- `app/services/goal_service.py`：+GOAL_RISK_DRAWDOWN_THRESHOLD(10)/
  GOAL_RISK_VOLATILITY_DAYS_THRESHOLD(3)/GOAL_NEAR_PROGRESS_THRESHOLD(50)、
  classify_goal_status；build_goal_analysis 两个 return 分支 +status/status_label
- `app/api/candidate.py`（新）：GET/PUT /api/candidates/filter
- `app/api/fund.py`：candidates 端点 +db 依赖（**注意补 `from app.database.database import get_db`**，
  本阶段踩过 NameError）
- `app/api/ai.py`：candidate-analysis 改调 `get_candidates(20, db)`
- `app/main.py`：注册 candidate.router

前端：
- `frontend/index.html`：goal-progress-head 包裹链路文案+徽章（#goal-status-badge）；
  candidates 面板加 #filter-form（#filter-type-stock/#filter-type-mixed/
  #filter-min-return-1y/#filter-save-btn/#filter-msg/#filter-desc）与
  #candidates-ai-loading；panel-tip 改为按条件描述的通用文案
- `frontend/js/api.js`：+getCandidateFilter/saveCandidateFilter
- `frontend/js/dashboard.js`：+loadCandidateFilter/buildFilterDescText/updateFilterDesc/
  saveCandidateFilter；loadCandidates 成功后 updateFilterDesc(screening_rule)；
  runCandidateAnalysis 改用 #candidates-ai-loading（finally 隐藏）；
  loadGoalAnalysis 渲染状态徽章（status → level-* 类名映射）
- `frontend/js/app.js`：+loadCandidateFilter() 初始化、filter-save-btn 绑定
- `frontend/css/style.css`：+.level-badge.level-neutral、.goal-progress-head、
  .filter-row/.filter-label/.filter-check/.filter-input、640px 媒体查询（输入框与按钮满行）

### 四、测试结果：pytest 164 passed（143 + 21 新增）

- `tests/test_candidate_filter_api.py`（新 6）：默认条件 / 保存重读+单行 / 全不勾 422 /
  下限越界 422×2 / 保存后候选池按条件过滤（含规则文案断言）
- `tests/test_candidates_api.py`（+3）：收益下限过滤空池+data_issues / 部分保留不足不补位 /
  仅勾混合型只请求 hh（类型跟随 ft 参数）
- `tests/test_goal_api.py`（+12）：classify_goal_status 纯函数 7 例（含边界 50% /
  风险优先于达标 / 均无数据不触发风险）+ API 集成 5 例（not_set / far / near 81.6% /
  achieved 无卖出文案 / risk 优先：achieved=true 且波动 3 天仍 risk_attention）

### 五、浏览器验收（真实库 + 真实 AI gpt-5.6，2026-09-23）

- **桌面 1280（13/14 + 聚焦复验 6/6，实际全过）**：
  筛选控件默认回填（全勾+0）与「默认条件」说明、保存后说明更新为下限 10%、
  加载候选池 20 只且规则文案含"10.00%""不构成任何推荐"、AI Loading 文案含
  「正在分析 20 只候选基金」、AI 解读真实调用 53 秒完成渲染（入选原因 6 条）、
  **红线禁词零命中**、目标徽章 ∈ 5 状态（真实持仓命中「风险需要关注」）、
  目标面板无推荐/预测表述
  - 注：首轮唯一失败项为 AI 解读 150 秒超时——上游 API 波动（免费中转偶发慢），
    提高 280 秒上限后聚焦复验通过（53 秒），功能本身无问题
- **手机 375**：无横向溢出（0px）、筛选控件可见（折行布局生效）
- 回归写入行已清理：candidate_filters=0、investment_goals=0、真实持仓 2 行完好；
  无残留进程

### 六、遗留问题

1. AI 候选池解读耗时受上游波动影响大（实测 53s~超 150s），Loading 文案已含预期提示，
   但无进度条/阶段性反馈（可接受，后续可考虑 SSE 或分阶段提示）
2. 筛选条件只有两个可调维度（按用户确认的"简单"定位）；如需回撤/波动过滤，
   扩展 candidate_filters 表与过滤管线即可（模式已就绪）
3. rankhandler 依赖不变（见 Phase 12 遗留问题 1）

---

## ✅ Phase 14：v1.0 最终封版（2026-09-24 完成）

> 本阶段不新增任何业务功能，只做发布前最终检查、清理与验证。

### 一、全仓库最终检查

1. **旧名称**：grep fundpilot/FundPilot 共 22 处，全部属于允许三类——
   数据库文件名 `data/fundpilot.db` 及其注释（.env / .env.example / README / database.py）、
   开发进度.md 历史记录、docs/dev-notes/ 历史开发指令；对外名称全部为「钱程似锦 Qiancheng」
2. **敏感信息**：真实 Key 仅存在于 `.env`（AI_API_KEY，已被 gitignore）；
   README 中为 `YOUR_API_KEY` / `sk-xxxx` 占位示例；开发进度.md 中为脱敏假 Key
   （sk-TESTDEMO123456）；全仓库无其他真实密钥/密码/Token
3. **gitignore 验证**：已 `git init` 并跑 `git status --porcelain`——
   待跟踪仅 12 项（.env.example/.gitignore/README.md/app/docs/frontend/tests/requirements/
   run.py/start.bat/开发进度.md/data 占位）；`git check-ignore` 确认
   `data/fundpilot.db` 与 `.env` 均被忽略；无 .db/.log/__pycache__/.venv/.trae 混入

### 二、README 最终更新

* 核心功能补齐 Phase 12/13 交付：新增「8. 投资目标与目标收益分析」「9. 基金关注（候选池）」两节
* 核心技术亮点 pytest 数量 118 → 164；开发路线补 Phase 10-11 / 12 / 13 完成条目
* 截图章节：更新为最终版 7 张截图（dashboard 全页含新面板、新增 goal-candidates.png、
  智能监控描述改为风险详情）；截图均基于脱敏演示数据
* 删除「你的用户名」占位符（克隆命令改为 `<仓库地址>` 通用写法）；
  后续计划更新（移动端适配已完成、市场信息已部分实现）

### 三、清理

* 删除 `.pytest_cache/` 与项目内全部 `__pycache__/`（.venv 除外）
* 临时库 fresh.db 已删除；无其他临时文件残留
* tests/ 保留 15 个测试文件 + conftest + 2 个 manual_test（真实网络冒烟，有意保留）

### 四、最终验证

* **pytest -v：164 passed**（23s，无失败无错误）
* **首次启动检查（临时库模拟新用户）7 项全过**：新库自动建表 / 无持仓空列表 /
  AI 配置来自 .env 且脱敏 / 空状态引导 / 总览卡隐藏 / 页面标题钱程似锦
* **截图重拍（临时库 + 3 只虚拟持仓 + 假 Key）**：候选池加载 / AI 分析真实调用 /
  手动自动检查（真实 AI 日报）/ 假 Key 脱敏保存，7 张截图全部更新至 docs/screenshots/
* **真实库最终浏览器检查 14/14**：持仓 2 行 / 免责横幅置顶（y=92）/ 风险详情默认折叠可展开 /
  目标未设置引导 / 筛选默认回填 / AI 按钮可用 / 面板命名（风险详情/基金关注/投资目标）/
  无「智能监控」旧名残留（修复自动检查面板提示文案 1 处）/ 手机 375 无溢出

### 五、真实数据库确认

* portfolio_holdings：2 行真实持仓原样（000001 3000份/1.2、002974 1278.18份/1.9559）
* investment_goals：0（无测试目标）；candidate_filters：0（无测试筛选条件）
* web_ai_configs：1 行（用户自己的 Web AI 配置，Fernet 加密，属正常用户数据）
* monitor_snapshots：62 / daily_reports：1（真实运行数据，保留）
* 真实 API Key 未做任何改动

### 六、Git 状态与发布结论

* 已执行 `git init`（空仓库）；`git status` 干净（12 项待跟踪、0 敏感文件）
* **结论：可以执行 `git add → git commit → git push`**（push 前需先在 GitHub 创建
  远程仓库并 `git remote add origin <仓库地址>`；README 已无占位符需要替换）

---

## ✅ Phase 15：Agent + Tool Calling 基础架构（2026-09-24 完成）

### 一、交付内容

真正的 Agent 循环：用户提出基金分析需求 → 模型自主判断需要哪些数据 →
调用对应 Tool（OpenAI Tool Calling）→ 工具执行现有 service → 结果回填上下文 →
循环直至给出最终中文分析回复。不是固定流程的硬编码 API 编排。

- **Agent 循环上限 6 轮**（AGENT_MAX_ROUNDS，达到后 502 终止），单次模型调用
  max_tokens=1500（tool_calls 需要更多空间），temperature=0.3，timeout 沿用 60s
- **6 个只读 Tool**（app/agent/tools.py）：零业务逻辑，全部直接复用
  fund_service / portfolio_service 现有函数（缓存、Decimal 计算自动生效）
- **工具执行器永不抛异常**：参数非法 / 基金不存在 / 数据源不可用 / 未知错误
  全部转成 {"error": ...} 以 role="tool" 回给模型自纠，不打断循环
- **红线策略（用户确认）**：最终回复命中 FORBIDDEN_PREDICTIVE_PHRASES →
  整次拒绝抛 AIResponseParseError → 路由层 502，不做片段丢弃
- AI 配置复用 web_config_service（Web 优先回退 .env）；本阶段仅后端 API，
  不含前端对话 UI（第二阶段）

### 二、6 个 Tool 与现有 Service 的对应关系

| Tool | 现有实现 | 参数 |
|---|---|---|
| get_fund_detail | fund_service.get_fund_detail | fund_code |
| get_fund_history | fund_service.get_fund_history | fund_code, page, page_size(1-50) |
| get_fund_performance | fund_service.get_fund_performance | fund_code, period(7/30/90/180) |
| get_holdings | portfolio_service.list_holdings | 无 |
| get_portfolio_summary | portfolio_service.get_summary | 无 |
| get_holding_history | portfolio_service.get_holding_simulated_history | holding_id, period |

### 三、新增/修改文件

* 新增 app/agent/__init__.py、app/agent/tools.py（Tool 注册表 + 执行器）
* 新增 app/services/agent_service.py（Agent 循环 + AGENT_SYSTEM_PROMPT + 红线检查）
* 新增 app/models/agent.py（AgentChatIn / AgentToolCall / AgentChatResponse）
* 新增 app/api/agent.py（POST /api/agent/chat）
* 修改 app/main.py（仅 +2 行：import agent、include_router）
* 新增 tests/test_agent_api.py（15 个用例）
* **未动**：现有 services / calculator / 数据库 / 其余路由 / 前端——现有功能零改动

### 四、新增 API

* POST /api/agent/chat，请求体 {"message": "分析需求"}（1-2000 字）
  响应：{model, generated_at, answer, tools_used[{tool, arguments}], rounds, disclaimer}
  异常映射：空白消息 400 / 未配置 AI 503 / 上游失败·超轮数·红线命中 502

### 五、测试结果：pytest 179 passed（164 + 15 新增，21s 无失败）

覆盖：空消息 400 / 未配置 503 / 直接回答（0 工具）/ 持仓链路
（get_holdings→get_portfolio_summary，真实执行结果断言）/ 持仓历史回放 /
基金详情+表现（含 Decimal 收益率精确校验）/ 历史净值分页 / 参数自纠
（period=999→error→自纠成功；非法代码；缺必填参数）/ 不存在基金·持仓 →
error 回模型 / 红线 502 整次拒绝 / 最大轮数 502（恰好在 6 轮终止）/
上游异常 502；每轮上下文断言不含 API Key

---

## ✅ Phase 16：Agent 实战验收与稳定性增强（2026-09-24 完成）

### 一、交付内容

对 Phase 15 Agent 后端做完整验收（真实模型实战）+ 边界稳定性用例补充 + 质量修复。

* **新增 tests/manual_test_agent.py**（沿用 manual 测试约定：不进 pytest、
  TestClient 直连真实库与真实 AI，全程只读）
* **tests/test_agent_api.py 新增 11 个 P16 边界用例**（15 → 26 个）
* **质量修复 1 处**：app/agent/tools.py 区间常量与 AnalysisPeriod 枚举双份维护 →
  统一为枚举单一来源（PERIOD_ENUM / VALID_PERIODS 派生，schema enum 同源）

### 二、真实模型端到端验收（gpt-5.6，key_source=web，6/6 场景全过）

| 场景 | 结果 |
|---|---|
| 普通问题（无需工具） | 1 轮直接回答，0 工具 ✓ |
| 单工具（查 000001 信息） | get_fund_detail → 2 轮 ✓ |
| 多工具连续（持仓分析） | get_holdings → get_portfolio_summary ✓ |
| 参数不完整自纠（第一只持仓近一月） | get_holdings 拿 holding_id → get_holding_history ✓ |
| 工具异常（基金 999999） | error 回模型 → 如实转告 ✓ |
| 诱导预测与交易建议 | 模型自主连查 30/90/180 天表现后干净拒绝预测 ✓ |
| 数据库验收前后一致 | 持仓数据完全一致（全程只读）✓ |

### 三、P16 边界用例（mock，防回归）

红线位置钉死（工具内容含禁词 + 干净回复 → 200）/ 空回复 502 / 消息超长 422 /
未知工具列出可用项 / 参数非 JSON / 参数非对象 / 执行器未预期异常转 error /
page-page_size 边界 / period 非法值（含 None）/ 数据源整体 500 → error 转告 /
同参重复调用由 6 轮上限兜底终止（上下文持续增长验证）
* 修复测试基建：ScriptedModel 需存 messages 快照（原引用被 run_agent 持续 append）

### 四、测试结果：pytest 190 passed（179 + 11 P16，无失败无回归）

---

## ✅ Phase 17：前端 AI 助手对话面板（2026-09-24 完成）

### 一、交付内容

Phase 15 后端 Agent API 接入现有 Web 前端：Dashboard 新增「AI 助手」对话面板
（位于「基金关注」之后、「风险详情」之前）。Agent 判断 / Tool Calling / 红线检测
全部仍由后端负责，前端只发送问题与展示结果，零业务判断。

* 用户 / AI 消息气泡区分（右蓝左白）、Enter 发送（Shift+Enter 换行，
  中文输入法组词回车不误发 isComposing）、输入框高度自适应
* 安全 Markdown 渲染：先整体 HTML 转义再做有限转换（加粗 / 行内代码 /
  代码块 / 标题 / 列表），杜绝注入；其余动态内容一律 textContent
* 工作过程轻量展示：tools_used → 中文标签小徽章链（"已查询：查询持仓列表 →
  查询账户汇总"），不暴露底层 JSON；悬停可见原始工具名
* 加载态：气泡内三点跳动 + "已用时 N 秒"计时（15 秒后追加等待提示）
* 异常展示：502 红线拒绝 / 上游失败等后端 detail 原样入错误气泡；
  未配置 AI 时黄色警示 + 禁用输入（复用 getAIStatus 检查）
* 回答气泡内固定展示后端附带免责声明；面板底部注明两条口径限制
  （无多轮会话记忆 / 非实时数据）

### 二、新增/修改文件

* 新增 frontend/js/agent-chat.js（对话逻辑 + 安全 Markdown 渲染，约 330 行）
* 修改 frontend/index.html（+34 行 agent-panel 区块；脚本链加 agent-chat.js）
* 修改 frontend/js/api.js（+runAgentChat 封装）
* 修改 frontend/js/app.js（+1 行 initAgentChat()）
* 修改 frontend/css/style.css（+约 245 行 Phase 17 样式，含 640px 移动端适配）
* 新增 docs/screenshots/agent-chat.png、agent-chat-mobile.png

### 三、浏览器验收（进程内 uvicorn + playwright，真实库只读 + 1 次真实 AI 发送）

15/15 PASS：桌面 1280 十项（面板渲染 / 欢迎消息 / AI 配置态 / 无横向溢出 /
发送后清空+禁用 / loading 气泡 / AI 回答渲染 / 工具链徽章（真实 gpt-5.6
2 轮 1 次工具调用）/ 免责声明 / 输入恢复）+ 手机 375 五项（渲染 / 欢迎 /
配置态 / 无溢出 / 截图）+ JS 运行时零报错；AUTO_TASK_ENABLED=false 保证
真实库零写入；临时脚本已删、8017 端口确认无残留进程

### 四、测试结果：pytest 190 passed（前端无构建系统，纯静态 JS/CSS 无需构建）

---

## ✅ Phase 18：Agent 多轮对话 + 流式输出（2026-09-24 完成）

### 一、Session 方案（轻量内存会话，零持久化）

新增 app/services/agent_session.py：进程内 dict 存储，仅保留**最近 8 条消息
（4 轮问答）**，只存 user/assistant 文本（工具中间产物不入历史，已反映在回答里）；
TTL 30 分钟过期、会话总数上限 200（超限淘汰最久未访问）；无效/过期 session_id
静默新建并在响应中返回新 id。**只有整轮成功（含红线通过）才记入历史。**

- 请求体新增可选 session_id（留空新建）；响应新增 session_id 字段；
- 原有 6 个 Tool 与 Agent 循环逻辑零改动，历史消息置于 system 之后参与推理；
- 「新建会话」= 前端丢弃 id（旧会话由 TTL 自动回收）；「清空当前会话」= 只清屏幕
  保留上下文（如实提示）。

### 二、流式方案（SSE，红线优先）

- 新增 POST /api/agent/chat/stream（text/event-stream）；
- **run_agent_events 是唯一的循环实现**（async generator 产出事件），
  非流式 run_agent 变为它的包装——Phase 15/17 行为完全兼容；
- 事件：session / status(thinking·tool+label·composing) / tool_done /
  delta / done / error；工具执行阶段状态为真实实时推送；
- **红线安全设计（关键决策）**：最终回复先完整生成 → FORBIDDEN_PREDICTIVE_PHRASES
  检测通过 → 才分块流出（24 字符/块 + 20ms 间隔）——不存在"先流出违规文本
  再撤销"；被拒/失败轮不写会话、无任何 delta 流出。

### 三、新增/修改文件

* 新增 app/services/agent_session.py、tests/test_agent_session_stream.py（10 用例）、
  tests/manual_test_agent_stream.py（真实多轮流式 E2E，manual 约定）
* 修改 app/services/agent_service.py（run_agent_events + run_agent 包装 + 工具中文标签）
* 修改 app/api/agent.py（/chat 传 session_id + /chat/stream SSE 路由）
* 修改 app/models/agent.py（AgentChatIn/Response + session_id）
* 修改 frontend/js/agent-chat.js（fetch ReadableStream 消费 SSE + 渐进 Markdown 渲染
  + 新建会话/清空当前会话 + 阶段状态文字）、index.html（按钮 + 文案）、
  style.css（阶段状态/计时小字/会话提示条样式）

### 四、验证结果

* **pytest 200 passed**（190 + 10 P18：多轮上下文 / 指代机制 / 会话隔离 /
  无效 id 新建 / 历史截断+过期 / 流式正常事件序 / 工具+流式组合 / 流式异常 /
  红线流式零 delta 且不记录 / 失败轮不入会话）
* **真实 gpt-5.6 多轮流式 E2E（manual_test_agent_stream.py）全过**：
  第一轮"帮我看看基金 000001"→ 实时工具状态 + 12 个 delta 分块（首块 35.4s）；
  第二轮同会话"**它**最近90天表现怎么样？"→ 模型无提示自主调用
  get_fund_performance(fund_code='000001', period=90)——指代理解有工具调用铁证；
  回答正确、红线干净；验收前后持仓一致（全程只读）
* **浏览器验收 13/13 PASS**：真实两轮对话（第二轮指代理解命中）/ 流式 loading
  阶段文字 / 工具徽章 / 新建会话清屏 / 手机 375 无溢出 / JS 零报错 / 截图更新
  （agent-chat.png、agent-chat-mobile.png）；临时脚本已删、8017/8018 端口零残留

### 五、已知限制

1. 会话仅存进程内存：服务重启即失效（页面明示"闲置 30 分钟后自动失效"）；
2. 单进程方案：多 worker 部署时会话不共享（当前单用户本地系统无影响）；
3. 最终回复为"生成完整后分块流出"，非逐 token 流式（红线安全优先的权衡）。

---

## ✅ Phase 19：项目收口与最终体检（2026-09-24 完成，开发暂停点）

### 一、技术体检结果（清单逐项核对）

| 检查项 | 结论 |
|---|---|
| Agent Loop / Tool Calling / 6 Tool | P15-P18 测试 + 真实 E2E 双重背书，无缺陷 |
| Session 8 条 / TTL 30min | P18 单元 + 集成测试覆盖（截断/过期/隔离/无效 id） |
| SSE 流式 / 状态实时 / 中途断开 | 事件生成器单一实现；客户端断开由 StreamingResponse 取消传播，无资源滞留 |
| 红线检测 | 流式先检测后流出（零泄露），非流式 502，被拒轮不入会话（测试钉死） |
| HTTP 异常 / 参数异常 / Tool 异常 | 400/422/502/503 映射完整；工具异常转 error 回模型自纠 |
| 重复请求 | 前端 agentBusy 防重；后端同会话并发交错仅影响历史顺序、无数据损坏（单用户场景可接受，已文档注明） |
| 并发安全 | agent_session 有 threading.Lock；fund_service._cache 为 GIL 下无害弱一致缓存（最坏重复回源） |
| API Key / .env / 日志 | Key 仅 .env + Fernet 落库；上游错误先抹 Key；日志无敏感信息 |
| CORS / 接口暴露 | allow_origins=*（单机同源部署的 Phase 5 决策，注释说明保留）；Agent 全部只读接口 |
| 前端 Markdown 安全 | 先整体转义再有限转换；其余 textContent |
| 前端构建 | 纯静态 HTML/CSS/JS，无构建系统（如实记录） |

**修复 2 处**：① 删除 frontend/js/api.js 中死代码 runAgentChat（P18 起前端走 SSE）；② 版本号 0.1.0 → 0.2.0（main.py 两处）。

### 二、全量回归结果

* pytest **200 passed**（修复后全量，18s）
* 真实 gpt-5.6 E2E 重跑：
  * manual_test_agent.py 6 场景全过（无工具 / 单工具 / 多工具链 / 参数自纠 /
    工具异常转告 / 诱导预测干净拒绝），6 Tool 全覆盖，验收前后持仓一致
  * manual_test_agent_stream.py 多轮流式全过（第二轮"它"→ 自主调
    get_fund_performance(000001, 90)，指代理解 + 11 delta 分块 + 会话延续）
* 浏览器核心流程 **12/12 PASS**（核心面板 / 版本 0.2.0 / 真实流式对话 /
  桌面+手机无溢出 / JS 零报错）
* 数据库：全程只读，验收前后持仓逐字段一致

### 三、文档整理

* README 补全：项目简介提 Agent、核心技术亮点重写（Agent/会话/流式/红线）、
  新增第 10 章「AI 助手（Agent 架构 / Tool Calling 流程 / 6 工具表 /
  Session 机制 / SSE 机制 / 安全红线）」、项目结构更新至实际目录、
  开发路线补 Phase 14-19、"后续计划 AI Agent"标记已实现、截图补 AI 助手 2 张
* 开发进度.md：Phase 15-19 章节完整，交接说明更新至 Phase 1-19 / pytest 200

### 四、简历项目材料（严格基于真实实现）

**项目名称**：钱程似锦 Qiancheng —— 个人基金智能监控与 AI Agent 分析系统

**一句话定位**：基于 FastAPI + OpenAI Tool Calling 的个人基金分析平台，
内置可多轮对话、流式输出的基金分析 Agent，全链路 Decimal 精确计算与 AI 红线管控。

**技术栈**：Python / FastAPI（async）/ SQLAlchemy + SQLite / OpenAI SDK
（Tool Calling / SSE）/ httpx / Pydantic v2 / Decimal / 原生 HTML+CSS+JS + ECharts / pytest

**简历项目描述（4 条）**：
1. 设计并实现基于 OpenAI Tool Calling 的基金分析 Agent：6 个只读工具全部复用
   service 层（零业务逻辑重复），模型自主规划多轮工具调用，循环上限与
   "工具异常转 error 自纠"机制保证稳定性；真实模型 E2E 验证多场景工具链
2. 实现 Agent 多轮会话与 SSE 流式输出：轻量内存会话（TTL + 条数上限控制上下文），
   实测模型正确解析"它"等指代；自定义 SSE 事件协议实时推送工具执行状态，
   最终回复先过红线检测再分块流出，安全与体验兼得
3. 建立金融数据正确性与 AI 安全红线体系：全链路 Decimal + ROUND_HALF_UP 计算、
   AI 禁止自行算数、预测性/推荐性表述解析层强制拦截（整次拒绝）、
   API Key Fernet 加密落库全程脱敏
4. 工程化交付：asyncio 定时任务（无第三方调度依赖）、候选池量化筛选管线、
   200 项 pytest 全绿 + 真实模型端到端验收脚本 + 浏览器响应式验收（1280/375）

**面试可展开的技术点**：
- Tool Calling 循环设计：为什么工具直调 service 层而非 HTTP 自调用（代理劫持坑、
  缓存复用）；执行器"永不抛异常"的 error-back-to-model 自纠模式
- 流式与红线冲突的取舍：为什么最终回复选择"缓冲-检测-分块流出"而非逐 token 直出
- 会话上下文控制：为什么只存 user/assistant 文本、8 条上限的推导
- SSE 技术选型：POST 场景为何用 fetch ReadableStream 而非 EventSource
- Decimal 金融计算与 float 精度坑；Pydantic v2 Literal[int] 查询参数坑
- 东财公开接口无 SLA 的防御性设计（自动翻页、失败隔离、不伪造数据）

---

## ✅ Phase 20：大盘行情 Agent Tool（2026-09-24 完成）

### 一、交付内容

第 7 个只读工具 `get_market_index`：上证指数 / 深证成指 / 创业板指的
当前点位 / 涨跌点 / 涨跌幅 / 更新时间 + 市场状态（交易中 / 已收盘 / 休市，
按行情更新时间如实判断，不猜测交易日历）。可选参数 index 过滤单只指数，
未知指数 error 列出支持项。接入现有 Agent Loop / SSE / 多轮会话，前端沿用现有 UI
（状态实时显示"正在查询大盘行情"，徽章 + 欢迎语示例同步更新）。

### 二、数据源选型（重要记录）

**腾讯公开行情接口 qt.gtimg.cn**（GBK 文本协议，字段布局实测验证：
[1]名称 [3]点位 [30]时间 [31]涨跌点 [32]涨跌幅）。

⚠️ 选型过程：最初实现用东财 push2.eastmoney.com（JSON），实测本项目网络环境
直连与走代理均间歇性连接重置（RemoteProtocolError，连备用域 push2delay 也不通），
而腾讯源直连稳定且行情数据与东财完全一致——故切换为腾讯源。
解析层用正则提取引号内内容（行尾 '";' 残留会导致 float 失败的坑已修）。

### 三、新增/修改文件

* 新增 app/services/market_service.py、tests/test_market_tool.py（8 用例）
* 修改 app/agent/tools.py（+get_market_index 定义与执行器）、
  app/services/agent_service.py（+工具标签）、frontend/js/agent-chat.js（+标签、欢迎语）
* 修改 README（6→7 工具、数据源说明、Phase 20 路线）
* 未动：既有 6 个 Tool 业务实现、红线策略、前端 UI 结构

### 四、验证结果

* pytest **208 passed**（200 + 8：解析 / 数据源 500 / 字段缺失如实说明 /
  全缺汇总抛错 / 参数过滤 / 未知指数 / 数据源挂掉 error / SSE 集成含状态 label）
* 真实 E2E："今天大盘怎么样？"→ 模型自主调用 get_market_index，
  回答三指数点位与数据源逐位一致（3888.37 / 13316.97 / 3288.95），
  状态"今日已收盘"正确转述，8 delta 流式，10.5s 完成；数据源故障场景
  另有验证（模型如实转告"数据源请求失败"，不编造数字）
* 浏览器验收 5/5（流式状态「正在查询大盘行情」/ 回答含三大指数 / 徽章 /
  无溢出 / JS 零报错）；数据库全程只读；临时脚本已删、端口零残留

---

## ⏸️ 交接说明（暂停前的续接指引）

> 更新时间：2026-09-24（Phase 20 大盘行情 Tool 完成）。**下次打开本项目，先读这一节即可接上。**

### 当前状态

- Phase 1 ~ Phase 20 全部完成，pytest 208 passed；项目处于**可展示 / 可发布状态**
- Agent 全链路：Tool Calling（**7 只读 Tool**，含大盘行情）+ 多轮会话（内存 8 条/TTL 30min）
  + SSE 流式（红线检测后分块流出）+ 前端对话面板（v0.2.0）
- 红线为长期约束：禁预测性表述（先检测后流出）、禁交易指令、免责声明随内容展示、
  Agent 全程只读不写库
- 已知限制：会话仅内存（重启失效、多 worker 不共享）；回复非逐 token 流式
- **发布流程（唯一待办）**：GitHub 创建远程仓库 → `git remote add origin <仓库地址>` →
  `git add .` → `git commit` → `git push`（README 已含 Agent 章节与 9 张截图）
- **已按用户要求暂停开发**，不自动进入新功能阶段
- 封版检查详情见上方 Phase 14 章节；Phase 12/13 红线（AI 不输出交易指令、
  候选池非推荐、目标≠止盈、禁预测性表述、免责声明全局展示）为长期约束
- 工作流：用户拿 GPT 写的开发指令 → 我执行 → 完成后按指令要求的格式输出汇报
  → 更新本文件 → 暂停等下一阶段指令

### 下次打开怎么跑起来

```powershell
# 方式 1（推荐）：python run.py（自动打开浏览器）
# 方式 2：双击 start.bat
# 方式 3：.venv\Scripts\activate 后 uvicorn app.main:app --reload
```

- 首页：http://127.0.0.1:8000 ；接口文档：http://127.0.0.1:8000/docs
- AI 功能需 `.env`（已配置 AI_API_KEY / AI_BASE_URL / AI_MODEL），未配置时 AI 面板按钮自动禁用并提示
- 注意 IDE 里选择 `.venv` 的 Python 解释器（Python 3.14，位于 D:\conda 创建的虚拟环境）
- ⚠️ 若页面显示异常旧状态（如 AI 未配置），先执行 `Get-Process python | Stop-Process -Force`
  清掉残留进程再启动（原因见下方关键技术记忆第 9 条）

### 当前数据库内容（data/fundpilot.db，真实数据勿随便删）

| id | 基金 | 份额 | 成本价 |
|---|---|---|---|
| 1 | 000001 华夏成长混合 | 3000 | 1.2000 |
| 2 | 002974 广发信息技术联接C | 1278.18 | 1.9559 |

### 关键技术记忆（踩坑结论，勿重蹈）

1. 金融计算一律 Decimal + ROUND_HALF_UP，前端不重复计算收益
2. 东财 lsjz 带日期区间时每页固定 20 条，需 page_size=20 自动翻页
3. Pydantic v2 查询参数用 int 枚举（AnalysisPeriod），不要用 Literal[int]
4. 后端 5 分钟内存缓存：改代码后立即验证可能拿到旧缓存，等过期或重启
5. 前端加载顺序：api.js → charts.js → dashboard.js → app.js（app.js 提供全局工具函数）
6. AI 分析：模型只解读后端数据不计算；输出约定 JSON（带剥壳容错）；前端 textContent 渲染防注入；
   Key 全程只在服务端内存，status 响应 / prompt / AI 输出均不出现
7. .env 里 DATABASE_URL 留空（写 `DATABASE_URL=`）会覆盖代码默认值导致 SQLAlchemy 报错——
   不用时直接注释掉该行
8. AI 上游调用用 AsyncOpenAI，timeout 60s；openai SDK 已装（3.17.0，requirements 中 openai>=1.40.0 兼容）
9. **uvicorn --reload 孤儿进程坑（2026-09-22 实锤）**：reload 模式 = 父进程（reloader）+ 子进程（worker）。
   非正常退出（如直接关 IDE 终端、taskkill 只杀父进程）会留下孤儿 worker 继续占着 8000 端口，
   用旧内存配置响应请求，新服务绑定失败——表现为"改了 .env / 重启了还是老样子"。
   排查命令：`Get-CimInstance Win32_Process -Filter "Name='python.exe'" | Select ProcessId, CommandLine`
   （multiprocessing-fork 且 parent_pid 已死的即孤儿）；清理：`Get-Process python | Stop-Process -Force`
10. 智能监控（Phase 7）：规则阈值常量全部写在 `app/services/monitor_service.py` 顶部（改规则只改那里）；
    单日涨跌幅直接用数据源自带 JZZZL（HoldingResponse.nav_daily_change），回撤复用 calculator.calculate_max_drawdown
    （单基金近 90 天口径）；数据不足/接口异常写入 `data_issues` 字段如实说明，绝不伪造数据；
    mock 测试的净值日期要用 `date.today()` 动态生成（波动窗口按"今天-30 天"过滤，写死日期会随时间失效）
11. 自动化（Phase 8）：定时循环是 asyncio 协程（未用 APScheduler），lifespan 启动/停止，
    首轮在启动后 FIRST_RUN_DELAY_SECONDS（5 秒）执行、之后按间隔循环（首轮在循环协程内，
    天然不重复）；AI 日报完全复用 ai_service.run_analysis，"每天一份"靠
    daily_reports.report_date 唯一约束 + 生成前查询守护；定时循环里任何异常都不能逃逸
    （except 后继续下一轮），手动触发 POST /api/auto/run 与定时任务共用 run_auto_check；
    AUTO_* 配置在 .env / .env.example
12. Web AI 配置（Phase 9）：加密密钥只从 FUND_PILOT_SECRET_KEY 环境变量读（sha256→Fernet），
    换 secret 后旧密文解不开→自动忽略回落 .env；改配置优先级逻辑只动 web_config_service 的
    get_effective_ai_config；_call_model 保持单参数调用兼容（config 为可选参数）；
    tests/conftest.py autouse 禁用后台任务，新增 TestClient 用例不用再操心真实库被后台轮次污染

### 历史阶段汇报

各阶段完成时的详细汇报（给 GPT 的反馈）已在当时对话中输出；本文件各阶段章节
即为完整交付记录，GPT 需要上下文时可直接引用对应章节。

---

